# Progress Log

Use this file to record user requests, meaningful changes, verification results, and notable follow-up items. Keep entries concise, newest first, and include dates.

## 2026-06-18

- Implemented Phase 6 Source Extract Proposals backend foundation for the Agent-first evidence workflow. Added deterministic source extract previews from indexed Excel source regions/ranges, including generic table-range extraction and C-number/component-distribution extraction with exact cell refs; persisted reviewable `source_extract_proposals` in Memory/Postgres with accept/reject decisions and audit events; accepted source extracts can now draft normal source-backed chart proposal sets with immutable source snapshots while leaving dataset commits, ChartSpecs, and Manuscripts untouched. Updated API/schema/canonical docs and route coverage. Verification: `node --test backend/src/saas/routes/saasRoutes.test.js`, full `npm run codex:verify`, and `docker compose config` passed; `npm --prefix backend run test:postgres` skipped as expected without a configured Postgres test URL.

- Implemented Phase 5 Source Document Index backend foundation for the Agent-first evidence workflow. Workbook scan/import now creates persistent SourceDocument records, SourceRegion summaries, and bounded source index blobs for Excel cell-grid retrieval in Memory/Postgres stores; normal import runs and supplemental batch workers both index uploaded workbooks. Added `GET /api/projects/:projectId/source-documents`, `GET /api/source-documents/:sourceDocumentId/regions`, `POST /api/source-documents/:sourceDocumentId/query`, and `POST /api/source-documents/:sourceDocumentId/range`, with auth checks, cross-lab denial, cell-count caps, and public importRun scan summaries that omit full `cellGrid.cells` from project state. Updated API/schema/canonical docs and route coverage. Verification: `node --test backend/src/saas/routes/saasRoutes.test.js` and full `npm run codex:verify` passed.

- Added lightweight natural-language compare routing to the server project chat planner. Prompts like `compare reaction rate for Exp30 and Exp31` now deterministically resolve active compatible reaction-rate ObservationSeries, return a safe `compare_series` action card with `series_compare` AnalysisView params, and let the frontend create the AnalysisView plus chart proposal through the existing review path; missing or incompatible experiments return clarification instead of falling back to a generic query. Updated AgentPanel execution, API contract notes, and backend/frontend regressions. Verification: targeted SaaS route test, targeted `ProjectDashboard` AgentPanel test, and full `npm run codex:verify` passed.

- Added a clear frontend `Compare series` entrypoint for the Agent-first evidence workflow. Overview, Supplemental Workbooks manager, and Imported Browser now expose a deterministic compare flow over active ObservationSeries; the new modal groups compatible series, lets users choose experiments, creates a `series_compare` AnalysisView through the server API, derives a normal chart proposal set, and opens the existing Chart Review so Accept -> Create ChartSpec -> Manuscript insertion remains unchanged. Added server API helper coverage and focused UI tests. Verification: targeted `serverApi`, `ProjectDashboard`, and `GenericImportBrowser` tests passed; full `npm run codex:verify` passed.

- Implemented Phase 3 Dynamic Series Chart Rendering for the Agent-first evidence workflow. ChartSpec normalization now preserves explicit `labrat.chartSpec.v1.4` compare specs with `seriesScope`, `compatibleExperimentIds`, and `series[]`; ChartSpec validation checks analysis-view-backed series against observation fields in the dataset commit; AnalysisView-derived proposals can be accepted and converted into durable v1.4 ChartSpecs; generic Plotly preview renders v1.4 series specs as one trace per experiment with `chartView.selectedExperimentIds` filtering; Manuscript insertion stores selected experiment ids for series-backed compare ChartSpecs. Updated API and canonical dictionary docs. Verification: targeted `genericChartPreview`, `ManuscriptCanvas.history`, and SaaS route tests passed, and `npm run codex:verify` passed.

- Implemented Phase 2 Series Compare AnalysisViews for the Agent-first evidence workflow. Added persisted Memory/Postgres `analysis_views`, `POST/GET /api/projects/:projectId/analysis-views`, `POST /api/analysis-views/:analysisViewId/chart-proposal`, resolver validation over active ObservationSeries, AnalysisView summaries in project state, and chart proposal sets with `origin: "analysis_view"` plus multi-series metadata while preserving the normal proposal review boundary. Updated SaaS API/schema/canonical dictionary docs. Verification: `node --test backend/src/saas/routes/saasRoutes.test.js` and `npm run codex:verify` passed.

- Implemented Phase 1 ObservationSeries registry for the Agent-first evidence workflow. Reaction-rate supplemental observation sets now derive first-class `observationSeries` records with source refs, experiment links, stale-state metadata, project state/list API exposure, Memory/Postgres store support, and a Postgres migration; lightweight reaction-rate two-column supplements now normalize into observation sets instead of pseudo experiments. Updated ObservationSeries API/schema docs and targeted tests. Verification: `node --test backend/src/import/services/normalizer.test.js`, `node --test backend/src/saas/observationSeries.test.js`, `node --test backend/src/saas/routes/saasRoutes.test.js`, and `npm run codex:verify` passed.

- Added durable Codex long-task execution loop environment Files: AGENTS.md, doc/decisions.md, doc/task-checklist.md, doc/code-review.md, scripts/codex-preflight.mjs, scripts/codex-checkpoint.mjs, scripts/codex-verify.mjs, package.json. Verification: npm run codex:preflight; npm run codex:verify.

- Aligned the active documentation set with the Agent-first evidence workflow plan. Updated README, AGENTS, backend README, Architecture, Roadmap, canonical data dictionary, AI boundaries, SaaS API contract, SaaS schema target, backend API contract notes, decisions, task checklist, code-review checklist, and the source-understanding reference plan so the active direction is Evidence Graph primitives, ObservationSeries, AnalysisViews, controlled AgentRuns, Source Workspace retrieval, source extract proposals, and source-backed/analysis-view-backed ChartSpecs. Preserved current/local endpoint docs separately from planned Agent-first APIs and made no document deletions because the existing docs still carry useful active context. Documentation-only change; no tests were run.
- Rewrote `doc/plan.md` as the active Agent-first evidence workflow execution plan. The new plan makes LabRat's next milestone a controlled AI workflow agent over Evidence Graph primitives, Observation Series, Analysis Views, Source Workspace, AgentRun traces, proposal review, human-confirmed execution, source-backed/analysis-view-backed ChartSpecs, and Manuscript compare controls. It includes concrete APIs, data model additions, UI interactions, phased implementation steps, testing coverage, manual QA, verification commands, AI cost policy, guardrails, and deferred items. Documentation-only change; no tests were run.
- Added `doc/source-understanding-long-term-plan.md` as a detailed long-term architecture plan for source-aware workbook/document understanding. The plan explains the current dataset-first clarification failure mode, proposes a Source Workspace layer with source documents, regions, retrieval tools, extract proposals, source-backed chart proposals, UI/API/data model phases, MCP positioning, and the Exp30 C-number distribution acceptance scenario. Linked it from the README documentation map and added a Roadmap item for Source Understanding Workspace. Documentation-only change; no tests were run.
- Generated an Exp30 carbon-number distribution chart artifact from `Calculation Exp30.xlsx`. Used `Sheet1!Q68:AY69` (`Overall tots`) as the C1-C35 percentage source, created an auditable workbook with source-cell references plus a rendered chart preview/SVG under `artifacts/exp30_carbon_distribution/outputs/`, and verified the output table/rendered chart plus a formula-error scan. No app code changed and no npm tests were run for this artifact-only request.
- Added a dedicated `Accepted + pending` action to the Project Overview Charts card. It shows active chart counts on the card and opens the existing Review chart proposals modal in an active-only mode that displays accepted and pending/proposed proposals while hiding rejected proposals. Existing all-proposal review, focused Edit, Accept/Reject, and Create ChartSpec behavior remain unchanged. Added Overview and ChartReviewPanel coverage. Verified targeted `ProjectDashboard`/`BackendScanPanel` tests, full `npm test` passed with 28 files and 201 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Fixed generic Experiment Browser Date columns showing Excel serial values such as `45733`. The scan stage already preserved Excel `formattedValue`, but standard table parsing and normalization dropped it while Browser cells rendered raw values; parser/provenance/normalizer now carry formatted cell values, Date-like fields are typed as `date`, and GenericImportBrowser/accepted mapping rows use the shared date formatter so existing serial values display as ISO dates and date ranges normalize consistently. Added backend parser/normalizer and frontend Browser-row/component regressions. Verified targeted tests, `npm --prefix backend test` passed with 173 tests and 1 skipped optional Postgres test, `npm test` passed with 28 files and 200 tests, and `npm run build` passed with the existing large Plotly chunk warning.

## 2026-06-17

- Implemented server-backed multi-supplement batch import with persisted Memory/Postgres batch state, SSE progress, and in-process concurrency-limited processing through the existing scan, normalize, and relationship-preview pipeline. The frontend now accepts multiple supplemental workbooks, uploads them with capped concurrency, opens the Supplemental Workbooks manager with batch progress, supports Review, Retry, and Apply selected ready items, and shows a reusable AI thinking indicator during relationship/chart waits. Added batch API/schema docs, focused backend/frontend coverage, and a supplemental upload helper. Verified `npm --prefix backend test` passed with 172 tests and 1 skipped optional Postgres test, `npm test` passed with 28 files and 198 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Added a direct `Insert into Manuscript` shortcut after Lab Rat chat creates a ChartSpec. Chart action cards now expose the shortcut once a durable ChartSpec id exists; clicking it keeps the agent open, switches to Manuscript, and opens the existing `Insert approved chart` modal with the new ChartSpec preselected, preserving compatible-experiment selection and preview rules. Added AgentPanel coverage for the insert callback plus ManuscriptCanvas coverage for immediate and delayed chart-spec insert requests. Verified targeted ProjectDashboard and ManuscriptCanvas tests, full `npm test` passed with 27 files and 197 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Pulled latest `origin/main` (`b66876b`) into local `main`, preserving local supplement/chart fixes with backup branch `codex/pre-pull-main-backup` and retained stash `codex pre-pull main backup before origin-main b66876b`. Resolved conflicts in the imported browser path by keeping upstream experiment starring/column controls and local mapping-review/project-id wiring. Confirmed private workbook fixtures and generated artifacts remain untracked. Verified `node --test backend/src/charts/services/chartIntent.test.js`, `node --test backend/src/saas/routes/saasRoutes.test.js`, `npm --prefix backend test`, `npm test`, and `npm run build` passed with the existing optional Postgres skip and large Plotly chunk warning.
- Added a compact pending chart proposal list to the Project Overview Charts card. The card now reads only the latest chart proposal set, shows up to three `proposed`/unset-status proposals with type, confidence, and a small Edit action, and opens Review chart proposals focused on the selected proposal without changing proposal payloads or ChartSpec creation behavior. Added focused Overview and ChartReviewPanel tests. Verified targeted `ProjectDashboard`/`BackendScanPanel` tests, full `npm test` passed with 25 files and 179 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Added in-chat chart proposal review for the server-backed Lab Rat agent. Interpreted chart actions now retain the returned chart proposal set/proposal ids, let users accept the proposal directly in the chat card, then create a durable ChartSpec from the same card while preserving the existing Accept -> Create ChartSpec -> Manuscript path and using workspace refresh so unsaved Manuscript state is not overwritten. Added AgentPanel regression coverage for the chat accept/create flow. Verified targeted `ProjectDashboard` tests, full `npm test` passed with 25 files and 177 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Split project-state hydration so server data/review refreshes no longer overwrite unsaved Manuscript canvas state. `applyProjectState` is now the full open/switch-project path, while import apply, master refresh apply, supplemental apply, mapping regenerate/save, ChartSpec creation, and Agent action reloads use a workspace refresh path that updates dataset/import/mapping/chart slices while preserving the current manuscript slice. Added a pure merge helper and regression tests proving manuscript records are preserved during workspace refresh but replaced during full project load. Verified targeted `ProjectDashboard` tests, full `npm test` passed with 25 files and 176 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Added an editable Semantic mappings workflow after MasterTable upload. Project Overview now has a `Semantic mappings` card with accepted/total counts, Imported Browser has a direct `Edit mappings` entry, and the new `MappingReviewModal` lets users edit mapping status, canonical field, semantic role, and unit while keeping `valueType` read-only. Saves use the existing `PATCH /api/mapping-sets/:id`, reload project state, and do not create dataset commits or mutate raw `genericImports`; stale mapping sets warn and offer regeneration for the current dataset commit. Added frontend modal/overview/browser coverage and backend route coverage proving mapping payload edits persist and viewer users cannot patch mappings. Verified `npm test` passed with 25 files and 174 tests, `npm --prefix backend test` passed with 170 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning.
- Reworked the Supplemental Workbooks Overview card into a full manager modal. The Overview now shows `Add supplemental workbook` plus `Manage supplemental workbooks`; the manager uses a left workbook list and right detail pane with file status, linked experiments, observation/source/field counts, y fields, and reviewed edit paths. Pending supplemental import runs can reopen Import Review through `Continue review`, while applied files direct changes through reviewed supplemental import rather than raw mutation. Updated modal/card tests. Verified targeted `ProjectDashboard` and relationship-helper tests, full `npm test` passed with 25 files and 171 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Kept local real-data outputs out of Git. Added ignore rules for `artifacts/` and `backend/src/import/fixtures/real-workbooks/`, and changed the real-workbook route regression to run only when the local Excel fixtures are present so CI/clone workflows do not require private lab data. Confirmed `git status --short` no longer lists those local data/output folders, while `git status --short --ignored` shows them ignored. Verified targeted `node --test backend/src/saas/routes/saasRoutes.test.js` passed with 34 tests.
- Fixed natural chart interpretation for supplemental workbook experiment scoping. Prompts like `plot adjusted rate vs reaction time for Exp35` previously parsed the experiment token but still resolved same-named fields from the first matching supplement, and nonexistent prompts like `Exp999` could still produce a chart. Chart intent now scopes candidate fields by requested `ExpNN` tokens or selected experiment ids, derives `sourceImportIds` from the actual selected fields, and returns a clarification when no imported supplement data matches the requested experiment. Extended the real-workbook route regression test to cover unscoped Exp35 selection and Exp999 rejection. Verified direct backend repros plus `node --test backend/src/charts/services/chartIntent.test.js`, `node --test backend/src/saas/routes/saasRoutes.test.js`, `npm --prefix backend test`, `npm test`, and `npm run build`; full suites passed with the existing optional Postgres skip and large Plotly chunk warning.
- Added a Supplemental Workbooks list inside the Project Overview card so users can see applied and pending supplemental files instead of only a count. The new relationship helper recognizes supplement imports through explicit relationship metadata or observation sets, summarizes file name, supplement type, linked experiment labels, observation/field counts, status, and updated date, and also surfaces pending supplemental import runs that have been scanned or normalized but not applied. Added focused helper and Overview rendering tests. Verified `npm test` passed with 25 files and 170 tests, and `npm run build` passed with the existing large Plotly chunk warning; browser smoke was skipped because the Browser plugin control tool was not exposed in this session.
- Added the four real workbooks as backend regression fixtures under `backend/src/import/fixtures/real-workbooks/` and documented them with a fixture README. Extended `backend/src/saas/routes/saasRoutes.test.js` to upload `MasterTable_updated.xlsx`, then apply `Reaction_Rate_Exp33.xlsx`, `Reaction_Rate_Exp34.xlsx`, and `Reaction_Rate_Exp35.xlsx` as supplements through the server routes and verify chart interpretation uses each supplement's own source ids. Verified targeted `node --test backend/src/saas/routes/saasRoutes.test.js` passed with 34 tests, `npm --prefix backend test` passed with 170 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 166 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Ran a real-data backend smoke test with `MasterTable_updated.xlsx` plus `Reaction_Rate_Exp33.xlsx`, `Reaction_Rate_Exp34.xlsx`, and `Reaction_Rate_Exp35.xlsx` from the original labrat data folder. The backend scan/normalize path parsed the master as 61 experiments and identified all three reaction-rate workbooks as `reaction_rate_time_series` supplements linked to Exp33/Exp34/Exp35 with 0.95 confidence, then chart interpretation resolved `Reaction Time (min)` vs `Adjusted Rate (M/s)` from the correct per-Exp supplement imports. Generated `artifacts/real-supplement-chart-smoke/index.html`, `reaction-rate-backend-output.svg`, and `backend-output-summary.json` for visual review. Verification was the direct backend pipeline smoke script; no code or dependency changes were made for this request.
- Fixed multi-supplement chart review so different Exp supplemental workbooks no longer collapse onto the first supplement chart. Frontend project-state restore now selects the newest dated proposal/mapping/import records instead of assuming array order, ChartSpec duplicate detection keys accepted proposals by proposal set plus proposal id, and deterministic chart proposal ids include source scope to avoid cross-supplement decision collisions. Added backend route coverage for Exp30 plus Exp31 supplements and second-import chart source ids, frontend proposal ordering and ChartSpec duplicate tests, and generic preview coverage for multiple observation imports. Verified `npm test` passed with 24 files and 166 tests, `npm --prefix backend test` passed with 169 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented server-backed conversational LabRat action planning. Added `POST /api/projects/:projectId/agent/plan`, deterministic safe action plans for master upload, master refresh, supplemental workbook upload, chart proposal, chart interpretation, ChartSpec creation, and data-query requests, plus auth/cross-lab route coverage. Refactored the AgentPanel path in logged-in project mode to call the server planner, render confirmable action cards, execute uploads through existing file/import/normalize/relationship/refresh/apply APIs, persist chart proposals/specs only after card confirmation, and reload project state after mutations. Updated AI boundary/API/plan/roadmap docs. Verified `npm test` passed with 24 files and 163 tests, `npm --prefix backend test` passed with 169 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning.

## 2026-06-16

- Added three synthetic manual smoke-test workbooks under `public/templates/test-workbooks/`: `LabRat_Test_Master_Exp30.xlsx`, `LabRat_Test_Reaction_Rate_Exp30.xlsx`, and `LabRat_Test_Selectivity_Normalize.xlsx`, plus a README with upload order and chart prompts. Verified with the backend scan/normalize services that the master workbook parses as 4 experiments, the reaction-rate workbook parses as a `reaction_rate_time_series` observation set with 62 observations, and the selectivity workbook parses as 4 experiments with component selectivity fields. Also inspected/rendered the generated workbooks and scanned for formula errors; `npm run build` passed with the existing large Plotly chunk warning.
- Fixed interpreted chart proposal style loss that made hollow/no-line scatter prompts still preview as filled points with connecting lines in the Chart proposals UI. The project-scoped interpret route now copies ChartSpec v1.3 `axisOptions`, `renderStyle`, transforms, series, and calculation warnings onto the persisted proposal, while the frontend preview and backend ChartSpec creation both fall back to nested `chartSpecDraft` for already-queued older interpreted proposals. Added route and renderer regression coverage. Verified `npm --prefix backend test` passed with 166 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 162 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Added hollow-marker/no-line scatter intent support for ChartSpec v1.3. Natural chart prompts such as `hollow markers`, `open circles`, and `no connecting lines` now compile to `renderStyle.traceMode: "markers"` with `marker.symbol: "circle-open"`; AI ChartIntent v2 prompt guidance and marker-symbol normalization now preserve open-circle intents through backend validation, and generic Plotly preview coverage verifies the marker-only hollow scatter output. Changed `backend/src/charts/services/chartIntent.js`, `backend/src/charts/services/chartSpec.js`, `backend/src/charts/services/chartIntent.test.js`, and `src/charts/genericChartPreview.test.js`. Verified targeted backend/frontend chart tests, `npm --prefix backend test` passed with 166 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 161 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Fixed natural chart intent column-hint conflict handling when master and supplemental imports both contain the same Excel column letter. If an alias-resolved field already comes from the requested column, such as `reaction time(column F)` resolving to the supplemental `Reaction Time (min)` field, the backend now accepts that match instead of treating another import's column F as a conflict. Added regression coverage with a conflicting master column F plus the reaction-rate supplement. Verified `npm --prefix backend test` passed with 163 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 160 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented AI-first natural chart intent with validator-locked ChartSpec v1.3 output. Chart interpretation now accepts ChartIntent v2 JSON from the server-side AI path, keeps deterministic fallback, resolves natural axis phrasing and Excel column hints against real source refs, detects alias/column conflicts as clarification, supports `axisOptions` for log axes, supports controlled `renderStyle` including `excel_like`, and keeps allowlisted chart transforms for requests like proportional selectivity normalization to 100%. Generic Plotly preview now projects v1.3 axis/style hints into log axes, line/marker style, grid, legend, and trace mode. Verified the real `Reaction_Rate_Exp30.xlsx` prompt resolved without clarification to `Reaction Time (min)` vs `Adjusted Rate (M/s)`, `yScale: log10`, `style: excel_like`, and 58 paired points. Verified `npm --prefix backend test` passed with 162 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 160 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Promoted `Reaction_Rate_Exp30.xlsx`-style supplemental workbooks to dedicated reaction-rate time-series imports. Backend scan now flags matching standard tables as `reaction_rate_time_series`, normalize creates one `observationSet` with observation rows instead of pseudo experiments, relationship preview/apply links the set to the existing Exp30, project catalog/chart intent can resolve `adjusted rate vs reaction time`, and frontend import review plus generic chart preview render observation-set summaries and chart points. Verified the real workbook parsed as 1 observation set with 62 observations and chart intent resolved to `Reaction Time (min)` vs `Adjusted Rate (M/s)` with 58 complete paired points. Verified `npm --prefix backend test` passed with 158 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 159 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Made server project workbook uploads idempotent for duplicate same-name/same-content files. `POST /api/projects/:projectId/files` now reuses an existing `fileObject` with `200 { reused: true }`, still allows fresh import runs from that file, keeps new uploads at `201`, and cleans up race-created duplicate storage files on unique-constraint recovery. Added Memory/Postgres file lookup helpers, file reuse audit events, backend route coverage, and optional Postgres parity coverage. Verified `npm --prefix backend test` passed with 156 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 157 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Added Project soft delete. Projects can now be marked `status: "deleted"` through the existing project PATCH path, deleted projects are hidden from the default project list, and soft deletion writes a `project.delete` audit event while preserving imports, dataset commits, chart specs, manuscripts, and audit data. Added a low-priority `Delete project` action plus confirmation modal on the Projects dashboard, a frontend `deleteServerProject()` helper, API contract notes, and backend/frontend regression coverage. Changed `backend/src/saas/routes/saasRoutes.js`, `backend/src/saas/routes/saasRoutes.test.js`, `src/data/serverApi.js`, `src/data/serverApi.test.js`, `src/main.jsx`, `src/components/ProjectDashboard.test.jsx`, `src/styles.css`, `doc/saas-api-contract-v0.md`, and `doc/PROGRESS.md`. Verified `npm test` passed with 24 files and 157 tests, `npm --prefix backend test` passed with 155 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning.
- Split the server project upload UX into one `Master Dataset` card and one `Supplemental Workbooks` card. The master path now allows one active master-table append, then requires refresh/replace; supplemental workbooks use scan -> normalize -> relationship preview -> reviewed `supplement_import` apply and are no longer rendered as standalone Browser experiment rows. Added frontend relationship review UI, Overview/refresh filtering tests, Browser-row regression coverage, backend `master_table_already_exists` protection, and route coverage for repeated supplemental imports. Updated active plan/API docs. Verified `npm test` passed with 24 files and 155 tests, `npm --prefix backend test` passed with 154 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning. Browser smoke was limited because temporary local frontend servers returned HTTP 200 from shell checks but refused the in-app browser connection after startup.
- Implemented the first RAG-like structured retrieval and supplemental import backend pass. Added dynamic project data catalog construction from current dataset commits, natural-language `POST /api/projects/:projectId/data/resolve-query` returning validated ViewIntent drafts, `POST /api/import-runs/:id/relationship-preview` for supplement/replace/standalone proposals, and `applyMode: "supplement_import"` for linking extra workbooks such as `Reaction_Rate_Exp30.xlsx` to existing experiments without mutating parent commits. Added frontend server API helpers, route coverage for Exp30 reaction-rate supplement apply and query resolution, and updated API/state/data dictionary docs. Verified `npm --prefix backend test` passed with 154 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 152 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Cleaned and refreshed the active documentation set after reviewing all files in `doc/`. Rewrote `doc/plan.md`, `doc/ROADMAP.md`, and `doc/ARCHITECTURE.md` around the current server-first workflow hardening milestone; replaced the outdated local-first `doc/canonical-data-dictionary.md` with a server-first dictionary covering dataset commits, generic import `fields[]`, ChartSpec v1.2 transforms, stale chart specs, manuscripts, and legacy compatibility; renamed misleading `doc/server-project-migration-plan.md` to `doc/server-project-state-plan.md`; updated README/AGENTS/backend README references; and tightened backend/SaaS/AI docs where dataset commits, chart specs, and `accepted_draft` wording had drifted. Documentation-only change; no business code changed and no tests were run.
- Updated Manuscript chart insertion so approved chart specs start with no experiments selected, require an explicit compatible experiment selection before insertion, and open a Browser-style selector using accepted semantic mapping columns with incompatible imported rows visible but disabled. Added focused coverage for empty initial selection, mapped selector columns, disabled incompatible rows, saved chartView selections, and chart-spec switching reset. Verified `npm test -- src/components/ManuscriptCanvas.history.test.jsx` passed with 39 tests, `npm test` passed with 24 files and 151 tests, `npm run build` passed with the existing large Plotly chunk warning, and local browser smoke loaded the app without a frontend crash; live Manuscript modal smoke was limited by the current seeded project having no chart specs.
- Split Import/Refresh review from chart proposal review. `BackendScanPanel` now stays focused on backend scan, block/field review, normalized preview/apply, refresh diff, and Semantic mappings; One-chart prompt and Chart proposals moved into a dedicated `Review chart proposals` modal opened from Project Overview. Trimmed the Import Review modal copy and removed the old scanned-workbooks/parser-boundary/review-state sections from that flow. Changed `src/main.jsx`, `src/components/BackendScanPanel.jsx`, `src/styles.css`, `src/components/BackendScanPanel.test.jsx`, and `src/components/ProjectDashboard.test.jsx`. Verified `npm test` passed with 24 files and 150 tests, and `npm run build` passed with the existing large Plotly chunk warning. Local HTTP smoke briefly returned 200 for dev/preview servers, but in-app Browser verification was blocked because those temporary local servers refused the browser connection after startup; manual UI check should confirm the two modal flows visually.

## 2026-06-15

- Hid the topbar project switcher on the Projects dashboard so the `File` button and current lab/project context are not shown there. Updated Topbar regression coverage. Verified targeted `ProjectDashboard` tests passed and `npm run build` passed with the existing large Plotly chunk warning.
- Hid stale chart specs after workbook refresh while preserving audit history. Added backend response-time chart spec staleness decoration (`isStale`, `status`, `staleReason`) for project state and chart-spec list APIs, filtered stale specs out of project AI context, and added frontend active-chart-spec filtering for Overview counts, Import Review, and Manuscript insertion choices while existing manuscript chart blocks still render from `chartSpecSnapshot`. Added backend route coverage for refresh-created stale specs and frontend coverage for active counts, hidden stale insertion choices, and snapshot rendering. Verified `npm test` passed with 24 files and 148 tests, `npm --prefix backend test` passed with 153 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented short-term ChartSpec v1.2 chart flexibility. Added chart-local allowlisted transforms, `distribution_bar`, C-number component family detection/sorting, normalized selectivity stacked proposals, C-number distribution proposals, AI/deterministic intent handling for normalized and distribution prompts, transform-aware chart-spec validation, and frontend generic preview rendering for transformed stacked bars and C-number distribution traces. Updated chart API docs and added backend/frontend regression tests. Verified `npm --prefix backend test` passed with 152 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 145 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Hid project workspace tabs on the Projects dashboard. Topbar now renders `Overview` / `Browser` / `Manuscript` / `Refs` only when `workspaceMode` is not `dashboard`, even if an active project id remains selected after using File -> Projects. Added focused Topbar regression coverage. Verified targeted `ProjectDashboard` tests passed and `npm run build` passed with the existing large Plotly chunk warning.
- Added the frontend Overview workbook refresh flow. The Dataset card now exposes `Refresh workbook` beside `Import workbook` when a server project has a current dataset commit and committed imports; users choose the old import in a refresh target modal, upload a replacement workbook, reuse Import Review in refresh mode, review the backend `refresh-preview` diff, and apply with `applyMode: "replace_import"`. Added `previewServerImportRefresh`, expanded import apply payload support, refresh diff/no-change/conflict UI, focused API/component tests, and styling. Verified `npm test` passed with 24 files and 141 tests, `npm --prefix backend test` passed with 147 tests and 1 skipped optional Postgres test, and `npm run build` passed with the existing large Plotly chunk warning. Browser smoke was limited because the already-running `127.0.0.1:5173` frontend was serving older source that did not include this workspace change, and a temporary 5176 dev server could not be kept alive from the tool shell.
- Consolidated the top navbar project navigation into a Google Docs-style `File` menu. Removed the `Projects` tab from the main topbar tabs, kept the current lab/project as a readonly context chip, added `Projects` to the File menu, and kept existing lab/project selectors plus New project/Profile/Import workbook/Logout actions in that same menu. Exported `Topbar` for focused coverage and updated tests. Verified `npm test` passed with 24 files and 142 tests and `npm run build` passed with the existing large Plotly chunk warning. The stale dev server on 5173 still showed an older navbar during browser smoke; current build output and focused tests reflect the new File menu.
- Pinned Projects dashboard short-date formatting to English (`en-US`) so updated dates render like `Jun 15, 2026` instead of following a Chinese OS/browser locale. Added dashboard regression coverage. Verified targeted `ProjectDashboard` tests passed and `npm run build` passed with the existing large Plotly chunk warning.
- Removed the duplicate topbar project-actions trigger. The `...` button opened the same ProjectSwitcher menu as the lab/project context button, so the navbar now keeps only the lab/project button with a clearer accessible label. Cleaned the unused `.topbar-menu-button` CSS. Verified targeted `ProjectDashboard` tests passed and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented Chart Propose v2 backend ranking with field/data profiling plus validated AI chart intents. Added field profile and pair profile helpers, proposal scoring/dedup/ranking, AI intent proposal generation that sends only compact field/profile/project context and compiles intents through backend field resolution, and project-scoped profile/existing-chart context wiring. Proposals now include additive `origin`, `score`, `scoreBreakdown`, `insight`, and compact `aiIntent` fields while remaining ChartSpec-compatible and review-only. Tightened response measurement selection so condition fields such as temperature/pressure are not recommended as y-axis responses. Added backend tests for profiling, ranking, low pair warnings, AI intent compilation, unresolved AI intent rejection, and persisted project proposal payload compatibility. Verified `npm --prefix backend test` passed with 147 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Fixed the Projects dashboard duplicate open actions. The project list row now keeps row selection separate from a real `Open` button that calls the project open handler, and the duplicate right-side detail-panel `Open project` button was removed. Updated `src/main.jsx`, `src/styles.css`, and `src/components/ProjectDashboard.test.jsx`. Verified `npm test` passed with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Fixed the remaining Carbon Balance one-chart clarification issue by improving backend generic import numeric detection. The live stateless chart interpret endpoint already resolved `plot Carbon Balance (%) as a bar chart by experiment`, but project data with values like `96.4%` was classified as categorical, so the field appeared in clarification options while failing the numeric y-axis filter. `numberValue()` now recognizes percent-suffixed and comma-formatted numeric strings and treats common missing markers as empty; added chart intent regression coverage for percent-string Carbon Balance values. Changed `backend/src/import/services/genericImportContext.js` and `backend/src/charts/services/chartIntent.test.js`. Verified targeted `node --test backend/src/charts/services/chartIntent.test.js`, `npm --prefix backend test` with 141 passed and 1 skipped optional Postgres test, `npm test` with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented backend import refresh/replace semantics for changed lab data. Added read-only `POST /api/import-runs/:id/refresh-preview`, extended `POST /api/import-runs/:id/apply` with `applyMode: "replace_import"`, added dataset refresh diff/merge helpers that replace one active `genericImports[]` item in a new immutable dataset commit while preserving the parent commit, annotated replacement imports with refresh lineage metadata, and wrote `import.refresh_apply` audit events. Added route tests for refresh preview/apply, no-change rejection, stale parent conflict, missing target, repeated apply, and historical parent preservation. Updated SaaS API docs, plan, and roadmap. Verified `npm --prefix backend test` passed with 140 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Implemented a conservative UI dedupe pass for the import-to-chart workflow. Unified external import actions under `Import workbook`, removed duplicate empty/imported Browser import buttons, aligned Project Overview/Profile/Refs labels, added a compact Import Review workflow step strip, clarified Semantic mappings / One-chart prompt / Chart proposals as one linear review path, and made persisted one-chart prompts show a compact queued status instead of a duplicate proposal review surface. Changed `src/main.jsx`, `src/components/GenericImportBrowser.jsx`, `src/components/BlankOnboarding.jsx`, `src/components/BackendScanPanel.jsx`, `src/styles.css`, and focused component tests. Verified `npm test` passed with 24 files and 136 tests, `npm run build` passed with the existing large Plotly chunk warning, frontend HTTP smoke returned 200, and in-app Browser login smoke reached the Projects dashboard with no visible legacy `Upload data` / `Import Excel workbook` / `Import more data` labels.
- Added Docker Compose development orchestration so LabRat can start with `npm run dev:docker` instead of hand-setting backend environment variables. Added `docker-compose.yml` with Postgres, backend, and frontend services; backend waits for Postgres health, runs migrations, seeds dev accounts, and serves on `8787`; frontend serves on `5173` with a Docker-aware `VITE_LABRAT_API_PROXY_TARGET`. Added root npm scripts for Docker stack lifecycle and updated README/backend README startup docs. Verified `docker compose config`, `docker compose up -d`, backend health, `labuser` login, frontend HTTP 200 at `/LabRat/`, `npm test` passed with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning. Compose logs still show existing npm audit warnings from current dependencies.
- Deployed a local Docker Postgres backend for LabRat. Created and started `labrat-postgres` on port 5432 with persistent Docker volume `labrat_pgdata`, ran `npm --prefix backend run migrate`, seeded `admin / LabRatAdmin123!` and `labuser / LabRatLab123!`, and restarted the backend on the frontend default proxy port `8787` using `DATABASE_URL=postgres://labrat:labrat_dev@127.0.0.1:5432/labrat`. During deployment, fixed `PostgresSaasStore` JSONB writes by serializing object/array payloads before insertion/update; this resolved `invalid input syntax for type json` during import apply. Verified Docker Postgres is running, `labuser` login succeeds, a real upload -> normalize-preview -> apply smoke creates a dataset commit, then cleaned the temporary smoke/debug projects from the database. `npm --prefix backend test` passed with 138 tests and 1 skipped optional Postgres test, `npm --prefix backend run test:postgres` passed against Docker Postgres, `npm test` passed with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Fixed the natural-language chart intent backend parser so prompts like `plot Carbon Balance (%) as a bar chart by experiment` resolve to a ChartSpec draft instead of asking for measurement clarification. The parser now recognizes `plot Y as/in chart type` and `make a chart of Y` phrasing, treats `by/per/across experiment` as an experiment x-axis request, and falls back to matching any real measurement alias from the field inventory rather than only hard-coded selectivity/conversion/yield names. Added a backend regression test with Carbon Balance fields. Changed `backend/src/charts/services/chartIntent.js` and `backend/src/charts/services/chartIntent.test.js`. Verified targeted `node --test backend/src/charts/services/chartIntent.test.js`, full `npm --prefix backend test` with 138 passed and 1 skipped optional Postgres test, `npm test` with 24 files and 136 tests, and `npm run build` passed with the existing large Plotly chunk warning.
- Replaced the topbar subtitle's hard-coded Y offset with a CSS-variable formula based on half the wordmark/subtitle font-size difference, so the current 20px/13px pairing computes to `-3.5px` while preserving the `LabRat` wordmark position. Changed `src/styles.css`. Verified `npm run build` passed with the existing large Plotly chunk warning.
- Nudged the topbar subtitle `Your AI Research Assistant` upward by 2px for optical alignment while leaving the `LabRat` wordmark position unchanged. Changed `src/styles.css`. Verified `npm run build` passed with the existing large Plotly chunk warning.
- Implemented Backend ChartSpec v1 optimization for complex chart proposals. Added a shared backend ChartSpec v1 compiler/normalizer with `point`, `grouped_bar`, and `stacked_bar` support; strengthened chart intent field aliases and selectivity family resolution; added deterministic grouped/stacked Solid/Liquid/Gas selectivity proposal recipes; filtered previously rejected proposal ids from new proposed outputs; normalized `chart_specs.spec` before persistence; exposed family metadata in project AI context; and made generic chart preview render `point` through the scatter path. Updated chart API docs and added backend/unit/route regression tests for point plots, grouped/stacked selectivity bars, validation failures, and persisted ChartSpec v1 payloads. Verified `npm --prefix backend test` passed with 137 tests and 1 skipped optional Postgres test, `npm test` passed with 24 files and 136 tests, `npm run build` passed with the existing large Plotly chunk warning, and `npm --prefix backend run test:postgres` skipped cleanly without `LABRAT_TEST_DATABASE_URL`.
- Made project-scoped natural-language chart interpretation use the same review-to-chartSpec frontend path as automatic chart proposals. Persisted interpret results now populate the shared chart proposal review state, update server project proposal sets by id, render preview/Accept/Reject/Create chart spec cards through `ChartProposalReview`, and leave preview-only local drafts clearly marked as server-only for chartSpec creation. Changed `src/main.jsx`, `src/components/BackendScanPanel.jsx`, and `src/components/BackendScanPanel.test.jsx`. Verified `npm test` passed with 24 files and 136 tests, `npm run build` passed with the existing large Plotly chunk warning, and in-app Browser smoke reached the LabRat login page at `http://127.0.0.1:5173/LabRat/`.
- Restored the `LabRat` wordmark's original vertical position after the subtitle alignment tweak. Kept `Your AI Research Assistant` expanded on one line beside it by aligning the subtitle with matching line-height and no vertical transform, without changing the wordmark transform. Changed `src/styles.css`. Verified `npm run build` passed with the existing large Plotly chunk warning.
- Adjusted the topbar brand subtitle so `Your AI Research Assistant` stays expanded on one line beside the logo and aligns with the bottom of the `LabRat` wordmark instead of wrapping vertically. Changed `src/styles.css`. Verified `npm run build` passed with the existing large Plotly chunk warning. Browser re-login visual check was blocked by the current browser virtual clipboard/fill issue, so final pixel-level confirmation remains a quick manual glance.
- Implemented the Scientific Minimal Topbar pass. Replaced verbose global actions with compact tabs (`Projects`, `Overview`, `Browser`, `Manuscript`, `Refs`), a contextual Project Switcher dropdown for lab/project/profile/import/logout actions, a quiet `Ask` button, and page-level `Import workbook` actions in Browser/imported/empty states. Removed the global records badge and topbar import/logout/profile/new-project button wall, kept manuscript save in the manuscript workspace toolbar, and tightened mobile topbar layout to avoid page-level horizontal overflow. Changed `src/main.jsx`, `src/components/GenericImportBrowser.jsx`, and `src/styles.css`. Verified `npm run build` passed with the existing large Plotly chunk warning and `npm test` passed with 24 files and 134 tests. Browser smoke covered seeded login, Projects dashboard, opening a project, Project Switcher menu, Browser contextual import modal, `Ask` panel open, and 390px responsive topbar/menu behavior. Residual manual QA: full manuscript canvas interactions were not re-tested because this pass did not touch canvas behavior.
- Changed the Imported/Generic Experiment Browser outer table to use accepted semantic mappings as user-facing columns. The row derivation now treats only explicit `accepted` mappings as browser columns/detail mapped fields, excludes `accepted_draft` from the main Browser, preserves parser stats and QA details for the detail modal, and gives each row accepted mapping values with missing values shown as empty cells. The Browser table now keeps only Label/Source as fixed columns, renders dynamic accepted mapping columns, searches accepted mapping values, and shows guidance when no accepted mappings exist. Verified `npm test` passed with 24 files and 134 tests, and `npm run build` passed with existing large Plotly chunk warnings.
- Restored editable manuscript chart components for server chartSpec charts without reintroducing the legacy HDPE `makePlot()` path. ChartSpec manuscript blocks now carry `chartView` and `chartLayout`, insert through a Configure Chart View modal with experiment selection and live preview, render title/legend/plotArea/x-axis/y-axis layers through the existing nested SelectionFrame editing model, expose chart layout/view controls in the Inspector, and preserve the same filtered view/layout for overview previews, selected-chart assistant context, and PPTX export. Added generic chartView filtering plus experiment-option derivation in `genericChartPreview`, updated chartSpec insertion/layout tests, and verified `npm test` passed with 24 files and 133 tests, `npm run build` passed with existing large Plotly chunk warnings, and `npm --prefix backend test` passed with 130 tests and 1 skipped optional Postgres test.
- Added the login-default project management workspace. Logged-in users now land on a Projects Dashboard instead of auto-opening the first project, with lab navigation, project rows, workflow status chips, a project detail panel, and a New Project modal that collects name and description before opening the created project/profile chat. Added a project-level Overview tab with workflow cards for profile, dataset, charts, and manuscript next actions. Added focused dashboard/modal tests and guarded `main.jsx` mounting for component imports. Verified `npm test` passed with 24 files and 130 tests, `npm run build` passed with existing Plotly chunk warnings, `npm --prefix backend test` passed with 130 tests and 1 skipped optional Postgres test, and in-app browser smoke showed the Projects Dashboard plus New Project modal.
- Introduced a consolidated CSS design token layer for LabRat UI styling. Expanded `src/styles.css` `:root` into primitive palette tokens, semantic surface/text/border/status tokens, spacing/radius/control-height/layout/z-index/elevation tokens, and compatibility aliases for existing variables. Rewired high-frequency topbar, controls, chips, status pills, onboarding, login, cards, tables, modals, import review surfaces, generic detail rows, and Manuscript layout/canvas overview dimensions to use the new tokens while preserving existing behavior and visual direction. Verified `npm run build` passed with existing large Plotly/XLSX chunk warnings and `npm test` passed with 23 files and 127 tests.
- Implemented a bounded overnight UI/UX polish pass for the server-first LabRat blank workspace. Tightened global control states, focus rings, hover/disabled states, modal/table/empty/error/loading surfaces, topbar wrapping and mobile action overflow, Import Review workflow cards/actions/status chips, Browser/imported-table empty states, generic detail readability, and Manuscript canvas/inspector selection affordances without changing data shapes, APIs, scientific calculations, or dependencies. Changed `src/styles.css`, `src/main.jsx`, `src/components/BackendScanPanel.jsx`, `src/components/GenericImportBrowser.jsx`, and `src/components/ManuscriptCanvas.jsx`. Verified `npm run build` passed with existing large Plotly/XLSX chunk warnings and `npm test` passed with 23 files and 127 tests. Local HTTP smoke passed at `http://127.0.0.1:5173/LabRat/`; in-app Browser loaded the login page, signed in with the seeded dev account after starting the backend with dev seed env vars, and confirmed the logged-in Browser/imported data UI rendered without visible errors. Residual manual QA: in-app Browser did not successfully switch to the Manuscript tab during automation and a later new-tab responsive smoke timed out while attaching the webview, so canvas drag/resize/keyboard/chart-layer behavior still needs hands-on morning verification.
- Migrated Manuscript chart insertion/rendering to the server chart-spec workflow. Manuscript chart blocks now insert from approved `projectState.chartSpecs` as `chartSpecId`/`chartSpecSnapshot` references, render through the generic chart renderer, expose chartSpec-based selected-chart AI context, and export chartSpec-backed charts to PPTX without using the legacy HDPE `makePlot()` manuscript path. Added server manuscript create/patch API helpers and made logged-in project saves persist Manuscript state through server manuscript APIs. Updated tests for chartSpec insertion and manuscript API helpers. Verified `npm test` passed with 23 files and 127 tests, `npm --prefix backend test` passed with 130 tests and 1 skipped optional Postgres test, and `npm run build` passed with existing large Plotly/XLSX chunk warnings.

## 2026-06-14

- Implemented backend data-integrity hardening for server-first projects. Import-run normalize/apply now enforce lifecycle transitions, repeated apply returns `409 import_run_already_applied`, duplicate committed generic imports return `409 duplicate_import_already_committed`, and normalize processing failures mark runs as `failed`. Import apply now creates full merged dataset commits from the parent payload plus the accepted dataset patch while preserving immutable parent commits. Chart spec creation now requires a project-owned dataset commit and validates chart type, x/y source field ids, and source refs before saving. Added helper modules, route tests for merged commits/lifecycle/chart validation, an optional Postgres route parity test gated by `LABRAT_TEST_DATABASE_URL`, and `npm --prefix backend run test:postgres`. Updated SaaS API/schema docs, backend README, plan, and roadmap. Verified `npm --prefix backend test` passed with 130 tests and 1 skipped optional Postgres test, `npm test` passed with 23 files and 125 tests, `npm run build` passed with existing large Plotly/XLSX chunk warnings, and `npm --prefix backend run test:postgres` skipped cleanly without `LABRAT_TEST_DATABASE_URL`.
- Implemented the first frontend server project workspace pass. Added a server API helper layer for auth, labs/projects, project state, project import runs, mapping/chart proposal persistence, project chart AI, and chart specs; added a login-default app shell with lab/project selection, new project creation, server project state loading, and server-derived Browser datasets; added a reusable chat shell plus structured ProjectProfileChat that saves project background through `PATCH /api/projects/:projectId/profile`; routed workbook upload/scan/normalize/apply through project-scoped file/import-run APIs; persisted mapping decisions and chart proposal decisions to server APIs; added Create chart spec from accepted proposals; and expanded Import Review to show grouped-header structure proposals and editable field role/name/unit overrides. Added focused API/component tests. Verified `npm test` passed with 23 files and 125 tests, `npm --prefix backend test` passed with 126 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Local HTTP smoke passed for `http://127.0.0.1:5174/LabRat/` and seeded `labuser / LabRatLab123!` login; in-app Browser navigation to localhost was blocked by the browser client with `ERR_BLOCKED_BY_CLIENT`.
- Implemented project-scoped AI chart workflow APIs. Added a project AI context assembler that derives compact field inventory, accepted mappings, prior chart decisions, existing chart specs, manuscript summaries, and warnings from the current project dataset commit without exposing raw workbook payloads. Added `PATCH /api/projects/:projectId/profile`, `POST /api/projects/:projectId/ai/context`, `POST /api/projects/:projectId/charts/interpret`, and `POST /api/projects/:projectId/charts/propose`; local/dev `/api/charts/interpret` and `/api/charts/propose` remain compatible. Project chart interpret is preview-only unless `persistAsProposal` is true; project chart propose persists a chart proposal set by default; neither creates chart specs directly. Fixed deterministic chart intent parsing for `grouped by` prompts. Updated `backend/README.md`, `doc/saas-api-contract-v0.md`, `doc/plan.md`, and `doc/ROADMAP.md`. Verified `npm --prefix backend test` passed with 126 tests, `npm test` passed with 21 files and 114 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings.
- Implemented server-first project persistence enhancement. Projects now support editable `projects.metadata.projectProfile` experiment background, `GET /api/projects/:projectId/state`, project-scoped list APIs for files/import runs/dataset commits, mapping-set create/update/list APIs, chart-proposal-set create/update/list APIs, and consistent memory/Postgres store support. Project profile updates merge without erasing unrelated metadata, viewer/editor/cross-lab authorization is tested, mapping/chart review decisions persist separately from raw imports and commits, and project state aggregates all project-owned server records. Updated `README.md`, `AGENTS.md`, `backend/README.md`, `doc/saas-api-contract-v0.md`, `doc/saas-database-schema-v0.md`, `doc/server-project-migration-plan.md`, `doc/plan.md`, and `doc/ROADMAP.md` to reflect server-first project state and no old IndexedDB/`.labrat.json` compatibility migration requirement. Verified `npm --prefix backend test` passed with 118 tests, `npm test` passed with 21 files and 114 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings.
- Cleaned the active `doc/` folder after the SaaS/Auth backend foundation and chart-interpretation work. Removed the completed `doc/saas-auth-v0-next-step-plan.md` and redundant `doc/implementation-milestones.md`, rewrote `doc/ROADMAP.md` around the real next target of frontend server mode, and updated `README.md`, `AGENTS.md`, `backend/README.md`, and `doc/ARCHITECTURE.md` so active references point to `doc/plan.md`, `doc/ROADMAP.md`, the SaaS API/schema/migration docs, backend API contract, canonical dictionary, AI boundaries, and the progress log. Documentation-only cleanup; no business code changed and no tests were run.
- Implemented one-sentence chart interpretation into validated LabRat ChartSpec drafts. Added local/dev `POST /api/charts/interpret`, request/response schemas, deterministic prompt parsing fallback, optional AI intent parsing with validation against real field inventory, source-backed ChartSpec draft compilation, clarification responses for ambiguous prompts, multi-y selectivity draft support, frontend `interpretChartWithBackend()` helper, an Import Review "Ask for one chart" UI, and generic chart preview support for ChartSpec `sourceIds` / multi-y drafts. Existing `/api/charts/propose` remains compatible and review-only. Verified `npm --prefix backend test` passed with 114 tests, `npm test` passed with 21 files and 114 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Implemented the backend SaaS Auth v0 foundation. Added config/env guardrails, Postgres migration SQL plus `npm --prefix backend run migrate`, a Postgres-ready store, a dev/test in-memory store, password hashing, httpOnly session cookies, seed account support for `admin / LabRatAdmin123!` and `labuser / LabRatLab123!`, role checks, admin lab/user APIs, lab/project APIs, file upload records, persisted import runs, normalize preview/apply, dataset commits, chart specs from proposals, manuscript persistence, audit events, local file storage ignore rules, and focused SaaS route/config tests. Existing stateless import/chart endpoints remain compatible. Installed backend dependencies, including `pg`; npm reported one high severity audit issue during install, and `npm --prefix backend audit --json` could not retrieve advisory details from the registry in this environment. Verified `npm --prefix backend test` passed with 107 tests, `npm test` passed with 20 files and 108 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Prepared the SaaS Auth v0 documentation set, removed completed/stale next-step docs, and redirected agent context toward Postgres/auth/server project persistence. Added `doc/saas-auth-v0-next-step-plan.md`, `doc/saas-database-schema-v0.md`, `doc/saas-api-contract-v0.md`, and `doc/server-project-migration-plan.md`; deleted the completed `doc/experiment-browser-next-step-plan.md` and stale `doc/import-examples.md`; updated `README.md`, `AGENTS.md`, `backend/README.md`, `doc/plan.md`, `doc/ARCHITECTURE.md`, `doc/ROADMAP.md`, `doc/implementation-milestones.md`, and `doc/backend-api-contract.md`. No business code changed.
- Implemented Generic Excel Importer v2 Phase 1 for MasterTable-style generic Excel imports. The backend scanner now preserves workbook comments, style hints, hidden row/column hints, formulas, and merged cells where available; region scan summaries expose candidate types/confidence; standard-table parsing now flattens grouped/multi-row headers such as `Catalyst -> Loading (g)` and `Selectivity (%) -> Gas`; normalized generic imports now include source-backed role-based `fields[]` while mirroring only true `measurement` fields into `measurements[]`. Added deterministic field-role classification for identifier/material/condition/measurement/metadata/note/ignored roles, scan `structureProposals`, normalize request support for future approved structures/field role overrides/template ids, and frontend Browser/import-preview support for field/material/condition/measurement counts with legacy fallback. Semantic mapping, chart proposals, and generic chart preview now read `fields[]` when present and fall back to old measurement-only imports. Added grouped-header scanner/parser/normalizer regression tests and updated docs for the new contract. Verified `npm --prefix backend test` passed with 97 tests, `npm test` passed with 20 files and 108 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Implemented the Generic Data in Experiment Browser next step. Added a derived generic browser-row model for `dataset.genericImports[]`, accepted/`accepted_draft` mapping overlays for dynamic display fields, an Imported/Generic Experiment Browser view, and a source-backed generic detail modal that shows metadata, measurements, warnings, confidence, source refs, and source lookup details without routing generic rows through HDPE-only `DetailModal` or `makePlot()`. Added focused helper and UI tests for generic row derivation, mapping overlays, imported row display, empty imported state, and generic detail. Updated `doc/plan.md`, `doc/implementation-milestones.md`, and `doc/experiment-browser-next-step-plan.md` so the completed browser integration is recorded and the next active target is local dataset commit metadata plus mapping-driven browser polish. Verified `npm test` passed with 20 files and 108 tests, `npm --prefix backend test` passed with 94 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings. Local dev server responded with HTTP 200 at `http://127.0.0.1:5174/LabRat/`; in-app Browser navigation to that local URL was blocked by the browser client with `ERR_BLOCKED_BY_CLIENT`.
- Cleaned up the active planning docs after completing backend contract stabilization. Removed the completed `doc/backend-next-step-plan.md`, added `doc/experiment-browser-next-step-plan.md` as the next implementation guide for deriving generic Experiment Browser rows and source-backed generic detail, updated `doc/plan.md`, `doc/implementation-milestones.md`, `backend/README.md`, `README.md`, and `AGENTS.md` to point agents toward the browser-integration milestone, and kept backend contract details in `doc/backend-api-contract.md`. Documentation-only cleanup; no business code changed and no tests were run for this checkpoint.
- Stabilized the local backend generic import contract for Experiment Browser readiness. `POST /api/import/scan` now derives a deterministic content-based upload id and optional SHA-256 checksum instead of using `Date.now()`, so repeated uploads of the same workbook keep stable scan file ids and normalized import ids. Normalized generic experiments now carry experiment-level confidence while retaining source refs, warnings, metadata, measurement fields, HDPE no-mutation behavior, and optional checksum metadata for route-uploaded files. Added backend regression coverage for stable upload/import identity, experiment-level contract fields, full source-ref resolution across experiments/metadata/measurements, and chart proposal prior-decision preservation. Verified `npm --prefix backend test` passed with 94 tests, `npm test` passed with 18 files and 103 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Added `doc/backend-next-step-plan.md` as the local backend execution guide for the next AI agent, covering the current stateless Node backend baseline, objective, implementation procedure, public API boundaries, and required test coverage. Linked it from `doc/plan.md` and refreshed `backend/README.md`, which was still describing the backend as Phase 1 scan-only despite current scan, normalize, semantic-map, and chart proposal endpoints. Verified `npm --prefix backend test` passed with 92 tests.
- Cleaned up stale documentation after reviewing the current `doc/` set. Removed the obsolete `doc/archive/import-plan-history.md` historical plan dump and the now-empty `doc/archive/` folder, reduced root `PROGRESS.md` to a short pointer to `doc/PROGRESS.md`, refreshed `doc/implementation-milestones.md` into a compact completed/current/next/future checklist, and corrected outdated phase/future wording in `doc/plan.md`, `doc/import-examples.md`, `doc/canonical-data-dictionary.md`, `doc/ai-boundaries.md`, and `doc/backend-api-contract.md`. Added `doc/implementation-milestones.md` back to the README documentation map. Verified `npm test` passed with 18 files and 103 tests, `npm --prefix backend test` passed with 92 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Reframed the project documentation around LabRat as a reproducibility-first research command center. Updated `README.md`, `AGENTS.md`, `doc/ARCHITECTURE.md`, `doc/ROADMAP.md`, `doc/plan.md`, `doc/canonical-data-dictionary.md`, `doc/backend-api-contract.md`, `doc/ai-boundaries.md`, and `doc/implementation-milestones.md` to describe the target flow from raw files to import proposals, human review, dataset commits, methodology versions/recompute proposals, Experiment Browser, chart specs, manuscript canvas, and PPTX export. Clarified that dataset commits, methodology versions, provenance graph, cloud storage, and audit logs are target architecture concepts while the current local app still uses `dataset.genericImports[]`, mapping/proposal sets, IndexedDB persistence, and legacy HDPE browser paths. Verified `npm test` passed with 18 files and 103 tests, `npm --prefix backend test` passed with 92 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Optimized the project documentation structure. Archived the oversized historical import plan to `doc/archive/import-plan-history.md`, replaced `doc/plan.md` with a short current execution plan, added `doc/ARCHITECTURE.md` and `doc/ROADMAP.md`, updated `README.md` and `AGENTS.md` for the current blank-mode/backend-test reality, added Experiment Browser/generic data guidance to `doc/backend-api-contract.md`, `doc/canonical-data-dictionary.md`, `doc/ai-boundaries.md`, and `doc/implementation-milestones.md`, and changed root `PROGRESS.md` into a pointer to the durable `doc/PROGRESS.md` log. Verified `npm test` passed with 18 files and 103 tests, `npm --prefix backend test` passed with 92 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.

## 2026-06-10

- Implemented Blank New User Mode as an alternate Vite mode while keeping the existing demo/research mode unchanged. Added `VITE_LABRAT_DATA_MODE=blank`, `npm run dev:blank`, and `npm run build:blank`; blank startup now skips `public/labratData.json`, ignores legacy localStorage seed data when no saved project exists, creates a fresh neutral empty dataset with `experiments`, `sources`, `files`, `genericImports`, `genericMappingSets`, `genericChartProposals`, and `warnings` arrays, and still loads saved projects normally. Added blank-mode onboarding with the existing scan -> review -> normalize -> apply -> mapping -> chart proposal flow, made `Import Excel workbook` the primary blank action, kept the HDPE MasterTable folder import as a secondary legacy option, added example-only workbook templates under `public/templates/`, and documented the blank commands in `README.md`. Verified focused tests passed with 5 files and 17 tests, full `npm test` passed with 18 files and 103 tests, `npm --prefix backend test` passed with 92 tests, `npm run build` passed, `npm run build:blank` passed, and HTTP smoke checks for `npm run dev` and `npm run dev:blank` both returned 200 on temporary ports. In-app Browser UI automation was still blocked by the recurring Windows sandbox startup failure. The existing large Plotly/XLSX chunk warnings remain.
- Implemented Phase 3 semantic mapping and chart proposal workflow. Added backend `POST /api/import/semantic-map` and `POST /api/charts/propose`, deterministic generic-import field inventory, semantic mapping proposals, chart proposal generation, optional server-side Anthropic adapter fallback warnings, frontend API helpers, Import Review mapping/chart proposal review panels, read-only Plotly preview cards, and project persistence for `dataset.genericMappingSets[]` / `dataset.genericChartProposals[]` without mutating raw generic imports or HDPE experiments. Verified `npm --prefix backend test` passed with 92 tests, `npm test` passed with 14 files and 92 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Frontend dev server responded at `http://127.0.0.1:5173/LabRat/`; in-app browser automation was blocked by the recurring Windows sandbox startup failure, and background backend server start did not stay reachable despite foreground/test health checks passing.
- Started Phase 3 planning for generic imports to AI semantic mapping and automatic chart proposal, explicitly excluding automatic manuscript chart insertion. Updated `doc/plan.md` with the current Phase 3 run, task queue, mapping/proposal shapes, review/persistence rules, and acceptance criteria; updated `doc/ai-boundaries.md`, `doc/backend-api-contract.md`, `doc/canonical-data-dictionary.md`, and `doc/implementation-milestones.md` with matching Phase 3 proposal boundaries. Documentation-only checkpoint; no tests run.

## 2026-06-09

- Removed the Experiment Browser table staging UI for a cleaner structure: the per-row `+`/checkmark stage column and `Stage filtered` button were removed from `src/main.jsx`, while the underlying staged experiment state, Detail modal `Stage for manuscript` action, Manuscript chart usage, sorting, filtering, row selection, and export behavior remain unchanged. Verified `npm test` passed with 10 files and 81 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings.
- Implemented LRP-39 default PowerPoint template-based manuscript export. Added a LabRat default PPTX template asset with standard Office-style layouts, shared template master definitions, and a JSZip merge step that applies the template master/layout/theme parts to generated manuscript decks while binding manuscript slides to the `LabRat Figure` layout. Added `jszip` as a direct dependency and updated the export modal copy. Verified `npm test` passed with 10 files and 81 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Generated a three-slide export fixture, confirmed XML/rels parsed with 10 layouts and valid relationship targets, and confirmed Microsoft PowerPoint COM opened the file with 3 slides and 10 custom layouts. Fallback export was not triggered during verification. Confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Normalized Experiment Browser date display without rewriting embedded dataset JSON files. `src/utils/date.js` now handles ISO dates, month/day/year dates, full ranges, and short-start ranges such as `3/10-3/11/2026`; Browser and Detail views display normalized dates through a formatter while sorting still uses the normalized first date. Verified `npm test -- src\utils\date.test.js` passed with 7 tests and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Full `npm test` still fails in the unrelated ManuscriptCanvas font-size blur test.

## 2026-06-08

- Reworked manuscript toolbar/editor selection ownership in `src/components/ManuscriptCanvas.jsx` into a shared state machine that distinguishes active editor selection, gray toolbar preview, restoring-to-blue selection, toolbar handoff, and block switch transitions. The new flow now consumes the first empty-canvas click after toolbar preview to restore the same blue text selection without exiting edit mode, lets the second empty-canvas click exit normally, and routes font size, font family, color, bold/italic/underline, and alignment through the same restore contract. Expanded `src/components/ManuscriptCanvas.history.test.jsx` with focused coverage for second-click canvas exit, font-family restore, color restore, button-action selection retention, and block-switch edit teardown. Verified `npm test -- src/components/ManuscriptCanvas.history.test.jsx` passed with 34 tests, full `npm test` passed with 10 files and 81 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings. Attempted browser-side verification after starting `npm run dev`, but browser automation was blocked in this environment because the `node_repl` browser runtime failed with `windows sandbox failed: spawn setup refresh`.
- Stabilized manuscript font-size inactive-selection preview in `src/components/ManuscriptCanvas.jsx` so the editor now treats the gray preview state as the real toolbar handoff signal, flushes typed font-size drafts before canvas/block clicks, restores the same blue selection on empty-space return, and still lets another block win after committing a valid draft. Expanded `src/components/ManuscriptCanvas.history.test.jsx` to cover gray preview focus, empty-canvas restore, toolbar handoff persistence, and block-switch blur behavior. Verified `npm test -- src/components/ManuscriptCanvas.history.test.jsx` passed with 31 tests, full `npm test` passed with 10 files and 78 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Refined the manuscript font-size toolbar to a Google Docs-style horizontal `- [input] +` control in `src/components/ManuscriptCanvas.jsx`, keeping manuscript text selection owned by the editor while the toolbar input is used and restoring the same range on Enter/blur/Escape or no-op exit. Updated `src/styles.css` to replace the old mini-stepper with a grouped horizontal control and expanded `src/components/ManuscriptCanvas.history.test.jsx` with typed font-size blur/Escape/invalid-draft coverage plus the renamed increase/decrease controls. Verified `npm test` passed with 10 files and 71 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Reworked the manuscript font-size toolbar into a custom size control with an editable numeric field plus explicit up/down step buttons, all sharing the same selection-preservation path in `src/components/ManuscriptCanvas.jsx`. Added release handling for no-op toolbar focus, hid native number spinners in `src/styles.css`, and expanded `src/components/ManuscriptCanvas.history.test.jsx` with font-size selection, stepper, pending-caret, undo/redo, and selection-only coverage. Verified `npm test` passed with 10 files and 67 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Stabilized manuscript bold/italic/underline behavior in `src/components/ManuscriptCanvas.jsx` by separating pending caret style from selected-text style, adding mixed-selection summaries, and making toolbar toggles resolve against the live editor selection instead of stale toolbar state. Added focused regression coverage in `src/components/ManuscriptCanvas.history.test.jsx` for italic/underline undo, mixed bold normalization, and collapsed-caret pending styles, plus mixed-state toolbar styling in `src/styles.css`. Verified `npm test` passed with 10 files and 62 tests, and `npm run build` passed with the existing large Plotly/XLSX chunk warnings.
- Updated `doc/plan.md` with final Phase 2 completion notes, final verification summary, and deferred Phase 3 boundaries. Documentation-only checkpoint; no tests run because final verification is already recorded: backend tests passed with 73 tests, frontend tests passed with 10 files and 54 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked final Phase 2 docs complete in `doc/plan.md`.
- Ran the production frontend build as Phase 2 verification. `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked frontend build complete in `doc/plan.md`.
- Ran the full frontend Vitest suite as Phase 2 verification. `npm test` passed with 10 files and 54 tests. Marked frontend tests complete in `doc/plan.md`.
- Ran the full backend Node test suite as Phase 2 verification. `npm --prefix backend test` passed with 73 tests. Marked backend tests complete in `doc/plan.md`.
- Verified existing HDPE chart generation still works after Phase 2 normalization changes. Ran `npm test -- src/charts/makePlot.test.js`; it passed with 1 file and 6 tests. No changes were made to `src/charts/makePlot.js`. Marked HDPE chart compatibility complete in `doc/plan.md`.
- Verified the old HDPE `MasterTable.xlsx` importer still works after Phase 2 normalization changes. Ran `npm test -- src/data/masterTableImporter.test.js`; it passed with 1 file and 2 tests. No changes were made to `src/data/masterTableImporter.js`. Marked MasterTable importer compatibility complete in `doc/plan.md`.
- Added explicit project export/import compatibility coverage for generic imports. Tests now cover wrapped exported project files with `project.dataset.genericImports`, malformed legacy `genericImports` normalization to an empty array, and continued HDPE experiment preservation. Verified `npm test` passed with 10 files and 54 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked project export/import compatibility complete in `doc/plan.md`.
- Made `genericImports[]` an explicit normalized project dataset sibling in `src/storage/projectStorage.js` so applied generic import data is stored without changing HDPE `dataset.experiments[]`. Added project storage tests covering save/export record building, imported project normalization, legacy datasets receiving an empty generic import array, and preservation of HDPE curated fields. Verified syntax checks, `npm test` passed with 10 files and 52 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked generic project dataset storage complete in `doc/plan.md`.
- Added the explicit `Apply normalized data` action after normalized preview. Applying requires browser confirmation, appends `datasetPatch.genericImports[]` through `applyGenericImportPatch()`, avoids duplicate import ids, leaves HDPE `dataset.experiments[]` unchanged, marks the project dirty, and shows applied-state feedback. Added helper tests and component tests for the apply action. Verified syntax checks, `npm test` passed with 9 files and 48 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked the explicit apply action complete in `doc/plan.md`.
- Added normalized preview UI in the backend scan panel. Users can preview approved blocks via `POST /api/import/normalize`, see generic import/experiment/measurement/warning counts plus source/file details and raw normalized JSON, and the preview resets on new scans or review-decision changes. The preview does not apply or mutate active project data. Initial frontend tests caught duplicate approved-block labels in an assertion; adjusted the test to expect repeated toolbar/card labels. Verified `npm test` passed with 8 files and 44 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked normalized preview UI complete in `doc/plan.md`.
- Added approve/ignore controls for detected backend scan blocks in the import review UI. Each block now shows pending/approved/ignored status, `Approve` and `Ignore` buttons, active decision styling, and callbacks wired to App review state; the controls do not normalize, apply, or mutate active dataset data. Added component tests for control callbacks and active status rendering. Verified `npm test` passed with 8 files and 42 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked approve/ignore controls complete in `doc/plan.md`.
- Added frontend approved-block review state helpers and wired state through the import review path. Backend scan success now initializes review state from detected blocks; scan start/failure clears it; the state is passed into the backend scan panel boundary for the upcoming controls. Added focused tests for block id collection, approved/ignored exclusivity, pending decisions, and stale decision filtering. Verified syntax checks, `npm test` passed with 8 files and 40 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked frontend approved-block state complete in `doc/plan.md`.
- Added frontend `normalizeScanWithBackend()` API helper for `POST /api/import/normalize`, keeping a relative `/api/import/normalize` endpoint and returning the backend dataset patch without applying it. Added typed `ImportNormalizeApiError`, client-side validation for scan result and approved block ids, support for mapping overrides/user edits/custom endpoint/signal, and focused tests. Verified syntax checks and `npm test` passed with 7 files and 36 tests. Marked the minimal frontend normalize API helper complete in `doc/plan.md`.
- Added route-level backend normalization fixture coverage in `backend/src/server.test.js`. The new test scans a mixed workbook, posts approved standard-table, repeated block-table, and unknown blocks to `POST /api/import/normalize`, and verifies generic import experiments/measurements, provenance, unsupported-block warnings, and no direct `datasetPatch.experiments` mutation. Verified syntax checks and `npm --prefix backend test` passed with 73 tests. Marked backend normalization fixture tests complete in `doc/plan.md`.
- Added warnings for unsupported approved normalization blocks. Approved unknown regions now emit `unsupported_block_type` warnings and do not create generic experiments or measurements. Added ambiguous sparse fixture coverage proving unknown scan output remains warning-only during normalization. Verified syntax checks and `npm --prefix backend test` passed with 72 tests. Marked unsupported/unknown block warning behavior complete in `doc/plan.md`.
- Added focused provenance coverage for Phase 2 normalization. The backend normalizer tests now verify generic import `files[]` and `sources[]`, resolvable `sourceRef` values for experiments, metadata, and measurements, and source records carrying file id, file name, sheet, range, block id, and raw value for both standard-table and repeated-block outputs. Verified syntax checks and `npm --prefix backend test` passed with 71 tests. Marked the provenance preservation task complete in `doc/plan.md`.
- Implemented approved repeated block-table normalization for Phase 1 `experiment_block` output. Each approved block now becomes one generic experiment, block title/source becomes experiment provenance, block metadata becomes experiment metadata, and block table cells become generic measurements tied to that experiment. Added fixture-backed coverage for two repeated blocks, temperature metadata, measurement values, units, and source refs. Verified syntax checks and `npm --prefix backend test` passed with 70 tests. Marked the block-table normalizer task complete in `doc/plan.md`.
- Implemented the backend standard-table approved normalizer in `backend/src/import/services/normalizer.js` and wired `POST /api/import/normalize` to it. Approved `standard_table` blocks now produce `datasetPatch.genericImports[]` with one generic experiment per table row, generic measurements for non-empty cells, and source refs preserving file, sheet, cell/range, block id, and raw value provenance. Added fixture-backed tests for standard table normalization, mapping overrides, missing approved block warnings, and no direct HDPE `experiments[]` patch. Verified syntax checks and `npm --prefix backend test` passed with 69 tests. Marked the standard-table normalizer task complete in `doc/plan.md`.
- Added the backend `POST /api/import/normalize` route boundary in `backend/src/import/routes/importRoutes.js`. The route now accepts `application/json`, rejects invalid JSON and invalid approval requests, and returns a stable `labrat.importNormalize.v1` envelope with an empty `genericImports[]` patch plus a `normalizer_not_implemented` warning until parser-specific normalization is added. Added server route tests for content type, JSON parsing, validation, and valid-envelope behavior. Verified syntax checks and `npm --prefix backend test` passed with 66 tests. Marked the normalize route task complete in `doc/plan.md`.
- Added backend Phase 2 normalization schemas in `backend/src/import/schemas/normalizationSchemas.js` with request validation, approved-block id normalization, generic-import patch shaping, and `labrat.importNormalize.v1` response summaries. Added focused backend tests proving the schema accepts valid scan approvals, rejects wrong scan versions or empty approvals, and does not expose direct HDPE `experiments[]` mutations. Verified syntax checks and `npm --prefix backend test` passed with 62 tests. Marked backend normalization schemas complete in `doc/plan.md`.
- Defined the Phase 2 generic dataset extension shape in `doc/plan.md`, `doc/canonical-data-dictionary.md`, and `doc/backend-api-contract.md`. Generic approved imports will append under `dataset.genericImports[]` as `labrat.genericImport.v1` records with experiments, measurements, sources, files, warnings, confidence, and approved block ids, while existing HDPE `dataset.experiments[]` remains unchanged. Documentation-only checkpoint; no tests run.
- Started Phase 2 Approved Normalization by updating `doc/plan.md` with a Phase 2 task queue, backend/frontend behavior, and first-checkpoint inspection notes. Inspected the Phase 1 scan response, backend parsers, import route, project persistence, main import/export flow, and backend scan panel. Key decision: generic normalized imports should be additive under a dataset sibling field such as `genericImports[]`, keeping HDPE `dataset.experiments[]` intact for existing importer and curated charts. Documentation/inspection checkpoint; no tests run.
- Added Vite dev-server proxy wiring so frontend `/api` requests are forwarded to the Phase 1 backend scan service at `http://127.0.0.1:8787`. Kept `scanWorkbookWithBackend()` on the relative `/api/import/scan` endpoint and did not change backend parser logic, Phase 2/Phase 3 behavior, charting, storage, or the HDPE importer. Verified `npm test` passed with 6 files and 32 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings. Marked the Phase 1 proxy wiring fix complete in `doc/plan.md`.
- Marked the final Phase 1 checkpoint complete in `doc/plan.md` and recorded that Phase 1 is fully complete while Phase 2 normalization and Phase 3 chart/AI mapping remain deferred. Documentation-only checkpoint; no tests run because final verification is already recorded: backend tests passed with 15 files and 56 tests, frontend tests passed with 6 files and 32 tests, and `npm run build` passed with existing large Plotly/XLSX chunk warnings.
- Updated `doc/plan.md` with final Phase 1 completion notes, final verification summary, known dev-server EPERM blocker, deferred Phase 2/Phase 3 boundaries, and an updated acceptance checklist. Documentation-only checkpoint; no tests run because the final verification results are already recorded in the plan. Marked final Phase 1 notes complete in `doc/plan.md`.
- Ran the existing frontend Vitest suite as a final Phase 1 verification checkpoint. `npm test` passed with 6 files and 32 tests. Marked existing test suite complete in `doc/plan.md`.
- Ran the production frontend build as a final Phase 1 verification checkpoint. `npm run build` passed with the existing large Plotly/XLSX chunk warnings. Marked frontend build complete in `doc/plan.md`.
- Ran the full backend Node test suite as a final Phase 1 verification checkpoint. `npm --prefix backend test` passed with 15 files and 56 tests. Marked backend tests complete in `doc/plan.md`.
- Added an in-memory `MasterTable.xlsx` compatibility test for the existing HDPE browser import path. The test verifies master-table rows still produce HDPE-shaped experiments, registry provenance, local file inventory, linked `calculation`/`sweep`/`parr_data` file references, and null parsed related-workbook fields, plus missing-master rejection. `src/data/masterTableImporter.js` implementation was not changed. Verified `npm test`, `npm --prefix backend test`, and `npm run build` with existing large Plotly/XLSX chunk warnings. Marked the existing MasterTable importer verification task complete in `doc/plan.md`.
- Added a focused no-mutation test for the frontend backend scan path, confirming a scan request leaves existing LabRat local project/dataset keys untouched. Verified `npm test`, `npm --prefix backend test`, and `npm run build` with existing large Plotly/XLSX chunk warnings. Marked the scan-output no-mutation task complete in `doc/plan.md`.
- Expanded the frontend backend-scan panel into a formatted sheet/block review view. It now shows sheet layout, confidence, row/column counts, block counts, candidate headers, candidate metadata, block metadata, warnings, and source ranges while retaining raw JSON for inspection. Added component test coverage for those displayed fields; an initial test run caught duplicate text assertions and the test was adjusted to expect repeated summary/detail labels. Verified `npm test`, `npm --prefix backend test`, and `npm run build`; local dev-server browser smoke check remains blocked by the existing Windows/Vite `.vite-temp` `EPERM` startup error. Marked the scan detail display task complete in `doc/plan.md`.
- Added a minimal frontend backend-scan upload/debug surface inside the existing Import review modal. The new `BackendScanPanel` uploads one workbook through `scanWorkbookWithBackend()`, shows loading/error/file/result summary states plus raw scan JSON, and can be opened without a prior folder import. Added component tests for upload callback, loading, result debug output, and errors. Verified `npm test`, `npm --prefix backend test`, and `npm run build`; local dev-server browser smoke check was blocked by the existing Windows/Vite `.vite-temp` `EPERM` startup error. The scan result remains local React state and does not mutate the active dataset, staged experiments, manuscript blocks, storage, or HDPE importer. Marked the minimal frontend upload/debug surface complete in `doc/plan.md`.
- Added frontend `scanWorkbookWithBackend()` API helper for `POST /api/import/scan`, with `FormData` upload, custom endpoint/fetch/signal support, typed `ImportScanApiError` failures, and tests for upload/error/validation behavior. Scoped Vitest to frontend `src` tests after initial `npm test` failed by collecting backend `node:test` suites as empty Vitest suites. Verified syntax checks, `npm test`, `npm --prefix backend test`, and `npm run build` with existing large Plotly/XLSX chunk warnings. The helper does not mutate active project data or replace the existing HDPE importer. Marked the minimal frontend scan API helper complete in `doc/plan.md`.
- Expanded unknown fallback tests for block source provenance, per-region candidate header/metadata filtering, and warning behavior when no regions exist without changing parser implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only fallback test checkpoint. Marked ambiguous/unknown fallback tests complete in `doc/plan.md`.
- Expanded block table parser tests for title/source provenance, unknown-block source/warnings, no-region parser warnings, and table-only blocks without title/metadata without changing parser implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only parser test checkpoint. Marked block table parser tests complete in `doc/plan.md`.
- Expanded standard table parser tests for skipped blank rows, partial data rows with explicit null/missing-cell provenance, and header-only tables with block-level warnings without changing parser implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only parser test checkpoint. Marked standard table parser tests complete in `doc/plan.md`.
- Expanded layout classifier tests for raw-name repeated-header matching and weak block-signal fallback without changing classifier implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only classifier test checkpoint. Marked layout classification tests complete in `doc/plan.md`.
- Expanded key-value detector tests for equals-delimited single-cell metadata, source context propagation, adjacent key delimiter trimming, and false-positive guards for numeric keys, empty values, and pipe-delimited keys without changing detector implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only detector test checkpoint. Marked key-value detection tests complete in `doc/plan.md`.
- Expanded unit detector tests for bracket units, no-unit labels, signed decimal value units, numeric-only values, and unsupported scientific-notation value/unit strings without changing the conservative detector implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only detector test checkpoint. Marked unit detection tests complete in `doc/plan.md`.
- Expanded header detector tests for source references, parsed units in labels, and configurable `minTextCells` behavior without changing detector implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only detector test checkpoint. Marked header detection tests complete in `doc/plan.md`.
- Expanded region detector tests for empty inputs, stable sorted region ids from unordered cells, numeric summaries, and strict `maxBlankGap` behavior without changing detector implementation. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only detector test checkpoint. Marked region detection tests complete in `doc/plan.md`.
- Added direct `workbookScanner` tests for structural scan output: file metadata, sheet ids/names, used ranges, cell grid shape, regions, candidate headers, formulas, merged-cell ranges, empty-sheet warnings, and `.xls`/`.xlsx` type metadata. The tests also assert scanner output stays structural and does not include parsed `layout` or `blocks`. Verified syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only test checkpoint. Marked scanner tests complete in `doc/plan.md`.
- Added a generated ambiguous sparse-sheet Excel fixture with sparse text/numeric cells that should not classify as a reliable table. Added a pipeline-level fixture test proving it scans as `unknown`, returns warnings, and does not invent table rows. Verified fixture syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only fixture checkpoint. Marked the ambiguous sparse sheet fixture complete in `doc/plan.md`.
- Added a generated repeated block-table Excel fixture with two separated experiment blocks, title rows, temperature metadata, repeated unit-bearing headers, and data rows. Added a pipeline-level fixture test proving it scans as `block_table` with two `experiment_block` entries and preserved block-level provenance. Initial backend tests caught that a three-column header row could be a sheet-level key/value candidate; adjusted the fixture to four table columns under the existing conservative detector. Verified fixture syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only fixture checkpoint. Marked the repeated block table fixture complete in `doc/plan.md`.
- Added a generated clean standard-table Excel fixture helper under backend import fixtures, avoiding binary workbook commits while keeping the fixture deterministic and readable. Added a pipeline-level fixture test proving it scans as one `standard_table` block with unit-bearing headers and preserved cell provenance. Verified fixture syntax checks and `npm --prefix backend test`; frontend build was not run for this backend-only fixture checkpoint. Marked the clean standard table fixture complete in `doc/plan.md`.
- Added backend scan response shaping with a stable `labrat.importScan.v1` envelope, file/summary metadata, sheet row and column counts, normalized warnings, consistent block skeletons, and preserved source ranges/provenance. Wired shaping into `runImportScan()` after parser orchestration, added focused response tests, and updated pipeline assertions. Verified syntax checks and `npm --prefix backend test`; frontend build was not run because no frontend code consumes the backend scan endpoint yet. Marked final scan response shaping complete in `doc/plan.md`.
- Added backend `importPipeline` orchestration with `runImportScan()`, keeping `workbookScanner` focused on structural scan data while the pipeline handles layout classification and parser selection. Updated the scan route to call the pipeline and added orchestration tests. Verified syntax checks and `npm --prefix backend test`; marked import pipeline orchestration complete in `doc/plan.md`.
- Implemented backend `unknownParser` fallback behavior for unknown layouts. Unknown sheets now return `unknown_region` blocks with candidate headers/metadata and warnings without inventing table rows. Added direct unknown parser tests and route-level ambiguous-sheet coverage. Initial backend tests caught a classifier bug where sparse numeric sheets without headers could classify as `standard_table`; fixed by requiring a candidate header for standard-table classification. `npm --prefix backend test` passed after the fix, and the unknown parser task is marked complete in `doc/plan.md`.
- Implemented backend `blockTableParser` for repeated regions with titles, metadata before headers, table rows, block source, warnings, confidence, and value provenance carrying block ids. Workbook scan output now parses `block_table` sheets into multiple blocks. Added parser tests and generated route-level block workbook coverage; verified syntax checks and `npm --prefix backend test`; marked block table parser complete in `doc/plan.md`.
- Implemented backend `standardTableParser`, parsing candidate header rows into columns, data rows, ranges, source references, and value provenance. Standard-table scan output now includes parsed `blocks`; added parser tests and route-level coverage. Verified syntax checks and `npm --prefix backend test`; marked standard table parser complete in `doc/plan.md`.
- Implemented backend `layoutClassifier` for conservative `standard_table`, `block_table`, and `unknown` classification using region/header/metadata/repeated-header signals. Workbook scan output now includes sheet-level `layout`; added direct classifier tests and scan output coverage. Verified syntax checks and `npm --prefix backend test`; marked layout classifier complete in `doc/plan.md`.
- Added shared backend confidence helpers for bounded scores, reason normalization, and weighted signal scoring. Updated header/key-value detectors to use the shared helper. Verified syntax checks and `npm --prefix backend test`; marked confidence scoring helpers complete in `doc/plan.md`.
- Implemented reusable backend `provenanceTracker` helpers for cell, range, and key/value sources. Header candidates and candidate metadata now include source references with file/sheet/cell/range/raw-value details. Added provenance tests and scan output coverage; verified syntax checks and `npm --prefix backend test`; marked provenance tracker complete in `doc/plan.md`.
- Implemented backend `keyValueDetector` for conservative metadata detection from single-cell delimiters and adjacent key/value cells. Workbook scan output now includes sheet-level `candidateMetadata` with parsed units/values and source ranges. Added direct detector tests and scan output coverage; verified syntax checks and `npm --prefix backend test`; marked key-value detector complete in `doc/plan.md`.
- Implemented backend `unitDetector` for visible units in headers and simple numeric value/unit strings. Header candidates now include parsed label/unit fields, with tests for parenthetical, slash, percent, and value units. Verified syntax checks and `npm --prefix backend test`; marked unit detector complete in `doc/plan.md`.
- Implemented backend `headerDetector` for text-heavy rows followed by numeric data rows, added sheet-level `candidateHeaders` to workbook scan output, and added direct detector tests plus scan output coverage. Verified syntax checks and `npm --prefix backend test`; marked header detector complete in `doc/plan.md`.
- Implemented backend `regionDetector` with small blank-gap grouping, region summaries, and sheet-level `regions` in scan output. Added direct region detector tests for contiguous tables, one-row gaps, and separated blocks, plus scan output coverage. Verified syntax checks and `npm --prefix backend test`; marked region detection tasks complete in `doc/plan.md`.
- Added reusable Excel address/range helpers in `backend/src/import/utils/excelAddress.js` and updated cell scanner plus merged-cell resolver to use them. Verified syntax checks and `npm --prefix backend test`; marked Excel address/range helpers complete in `doc/plan.md`.
- Extracted reusable merged-cell resolver helpers into `backend/src/import/utils/mergedCellResolver.js` and updated `cellScanner` to use them without changing scan output. Verified syntax checks and `npm --prefix backend test`; marked merged cell resolver complete in `doc/plan.md`.
- Added explicit backend test coverage proving scan output preserves used ranges, cell addresses, raw/formatted values, formula text, and merged-cell range markers. Marked the preservation queue item complete in `doc/plan.md`; `npm --prefix backend test` passed.
- Added backend cell grid representation with `scanCells()`, including sheet range, row/column counts, non-empty cells, source addresses, raw/formatted values, formulas, types, and merged-cell markers. Wired cell grids into workbook scan output and updated backend tests. Verified syntax checks and `npm --prefix backend test`; marked cell grid representation complete in `doc/plan.md`.
- Implemented backend `workbookScanner` for Phase 1 scan metadata: valid Excel uploads now return file metadata, sheet ids/names, used ranges, and empty/no-sheet warnings. Added explicit backend `xlsx` dependency declaration and updated backend route tests to generate an in-memory workbook. Verified syntax checks and `npm --prefix backend test`; marked `workbookScanner` complete in `doc/plan.md`.
- Added backend Excel upload handling for `POST /api/import/scan`: request body reading, dependency-free multipart parsing, `.xlsx`/`.xls` validation, file metadata response, and rejection tests for missing/invalid uploads. The route still returns `501 scanner_not_implemented` after valid upload until workbook scanning is implemented. Verified syntax checks and `npm --prefix backend test`; marked upload handling complete in `doc/plan.md`.
- Added backend route boundary for `POST /api/import/scan`, a shared JSON response helper, server routing hook, and backend tests for `/health` plus the scan route. The scan route currently returns `501 scan_not_implemented` until upload handling is added. Verified with Node syntax checks and `npm --prefix backend test`; marked the route task complete in `doc/plan.md`.
- Added minimal backend server in `backend/src/server.js` with a JSON `GET /health`, JSON 404 fallback, and exportable `createServer`/`startServer` helpers. Verified with `node --check backend/src/server.js` and `npm --prefix backend test`; both passed. Marked the minimal server task complete in `doc/plan.md`.
- Added backend dev/start/test scripts to `backend/package.json` using Node built-ins. Verified `npm --prefix backend test` passes with no backend tests yet, and marked the scripts task complete in `doc/plan.md`.
- Added minimal backend package setup in `backend/package.json` with ESM enabled and no dependencies/scripts yet. Marked the package setup task complete in `doc/plan.md`; checks not run for this metadata-only checkpoint.
- Created the isolated `backend/` folder for the Phase 1 scan-only pipeline with a placeholder README. Marked the backend-folder task complete in `doc/plan.md`. No server code or dependencies added yet; checks not run for this folder/documentation-only checkpoint.
- Verified the current app commands before adding backend Phase 1 code. `npm test` passed with 3 files / 20 tests, and `npm run build` passed with the expected large Plotly/XLSX chunk warnings. Marked the command-verification and plan-status tasks complete in `doc/plan.md`.
- Began Phase 1 backend scan-only pipeline work by completing the first task-queue checkpoint: inspected current project structure plus import, chart, and storage boundaries. Marked the inspection task complete in `doc/plan.md` and documented current-state findings. No code behavior changed; checks not run for this inspection-only checkpoint.
- Finished `doc/plan.md` as the primary backend-import execution roadmap. Added current-state context, explicit architecture decisions, phase-by-phase backend/frontend behavior, completion gates, deferred work, AI safety rules, fixture expectations, and an acceptance checklist while keeping detailed examples/API contracts in companion docs. Documentation-only change; build not run.
- Moved the durable progress log from repository root `PROGRESS.md` to `doc/PROGRESS.md` so project planning/context docs live together. Updated `AGENTS.md` workflow references to the new path. Documentation-only change; build not run.
- Expanded backend-import AI context with companion docs: `doc/import-examples.md`, `doc/canonical-data-dictionary.md`, `doc/ai-boundaries.md`, `doc/backend-api-contract.md`, and `doc/implementation-milestones.md`. Updated `doc/plan.md` and `AGENTS.md` to cross-link the docs. Documentation-only change; build not run.
- Added `doc/plan.md` as durable AI-agent context for the preferred backend import architecture: backend scan-first pipeline, block-first Excel parsing, conservative provenance/warnings, later normalization, generic charts, and AI-assisted mapping. Updated `AGENTS.md` to point future agents at the plan for backend import, AI parsing, and generic experiment support. Documentation-only change; build not run.
- Unified experiment date formatting through a shared normalization layer instead of hand-editing the large dataset JSON. Added `src/utils/date.js`, normalized embedded/imported/project-saved datasets to consistent ISO-style dates (including date ranges like `2025-04-10 to 2025-04-11`), and updated browser/manuscript experiment sorting to use normalized date values. Verified with `npm run build`; build passed with expected large chunk warnings.
- Updated the local embedded dataset JSON to include `Exp61` after the new `extra data/GC Data + Sweep-selected/MasterTable_updated.xlsx` added that experiment. Parsed and merged `Exp61` master-table fields plus its calculation, sweep, reaction-rate, and Parr files into both local dataset copies (`public/labratData.json` and `src/data/labratData.json`), bringing the embedded dataset count to 61 experiments. Verified with a local dataset sanity check and `npm run build`; build passed with expected large chunk warnings.
- Changed reaction-rate chart Y-axis ticks to use scientific notation on the log scale instead of SI-prefix formatting. Updated `src/charts/makePlot.js` to set Plotly `exponentformat: "e"` and `showexponent: "all"` for rate charts, and added a focused assertion in `src/charts/makePlot.test.js`. Verified with `npm test` and `npm run build`; both passed with expected large chunk warnings.
- Removed the temporary `npm run generate:data` workflow to keep the app focused on directly consuming the local `public/labratData.json` file. Deleted `scripts/generateLabratData.mjs`, removed the script entry from `package.json`, and updated `README.md` / `AGENTS.md` so local usage and zip-sharing instructions now assume `public/labratData.json` is provided directly rather than regenerated from `extra data/`. Verified with `npm run build`; build passed with expected large chunk warnings.
- Switched the embedded dataset to a generated-local-artifact workflow so the repo no longer needs to track 100MB+ JSON files. Added `public/labratData.json` and `src/data/labratData.json` to `.gitignore`, removed both from git tracking with `git rm --cached` while keeping the local files intact, added `npm run generate:data`, and changed `scripts/generateLabratData.mjs` to emit only the runtime-used `public/labratData.json`. Updated `README.md` and `AGENTS.md` to reflect that runtime loads from `public/labratData.json` and that this file is a local generated artifact. Verified the local data files still exist and `npm run build` passed with expected large chunk warnings.
- Formalized chart behavior around the workbook plotting rules without changing the stable Excel importer or embedded dataset files. Updated rate-chart semantics in `src/charts/makePlot.js` with explicit sweep/non-sweep classification from structured signals, preserved mixed sweep+nonsweep charts under a generic `Reaction Rate (M/s)` log-scale Y axis, and kept negative reaction-time points intact. Aligned default rate x-axis naming in `src/charts/chartLayout.js` from `Time (min)` to `Reaction Time (min)`. Added focused chart tests in `src/charts/makePlot.test.js` covering Exp29-like sweep, Exp9-like non-sweep, mixed charts, negative time points, and 100%-basis selectivity normalization. Verified with `npm test` (16 tests passed) and `npm run build` (passed with expected large chunk warnings).
- Refactored Manuscript undo/redo into one explicit snapshot-based transaction manager: added `src/utils/manuscriptHistory.js` and `src/components/useManuscriptHistory.js`, rewired `src/components/ManuscriptCanvas.jsx` so text edits, formatting, block move/resize/delete, page add/delete, and block insertion all flow through the same document-state history (`blocks`, `pages`, `canvasHeight`, `pageOrientationPreference` only), and extended `SelectionFrame`/`geometry` with drag-resize lifecycle hooks plus shared keyboard undo/redo routing. Added a compact Vitest + RTL regression suite in `src/components/ManuscriptCanvas.history.test.jsx` covering typing/paste/format undo, exact `textRuns` redo, drag/resize/delete, page add/delete, redo clearing, selection-only no-op history, keyboard shortcuts, and text-then-move undo ordering. Added `vitest.config.js`, `src/test/setupTests.js`, and the `npm test` script using `--configLoader runner` to avoid the Windows `.vite-temp` EPERM issue. Verified with `npm test` (10 tests passed) and `npm run build` (passed with expected large chunk warnings). Attempted an in-app browser smoke check, but the recurring Windows browser sandbox startup failure prevented local browser verification in this turn.
- Confirmed `extra data/` is not part of the current runtime or normal build contract: default startup still loads embedded data from `public/labratData.json`, the live `Load Excel folder` path still depends on user-selected files passed into the stable master-table importer, and only the optional dev-only dataset generator script references the repository `extra data/` folder. Added `extra data/` to `.gitignore`. Verified `git ls-files -- "extra data"` is empty so no cached untrack removal was needed, and `npm run build` still passed with expected large chunk warnings.

## 2026-06-07

- Repaired the Manuscript rich-text undo/redo lifecycle by replacing the loose pending text batch with an explicit text edit session that captures the pre-edit manuscript snapshot, tracks dirty `textRuns`, commits only changed sessions, and flushes on blur, save, selection changes, non-text edits, and undo/redo. Added manuscript-routed `Ctrl/Cmd+Z`, `Ctrl/Cmd+Y`, and `Ctrl/Cmd+Shift+Z` handling inside text boxes so contentEditable browser history cannot bypass `textRuns`. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Refactored Excel ingestion into clean layers. The active import flow now uses a dedicated stable master-table importer (`src/data/masterTableImporter.js`) that only parses the selected master table and attaches experiment-linked `calculation`/`sweep`/`parr_data` file references without parsing workbook contents. Added a structural workbook scanner (`src/data/workbookScanner.js`), an AI parser stub boundary (`src/data/aiExcelParserBoundary.js`), and a light Import review modal in `src/main.jsx` that shows scanned workbook metadata and parser stub status while keeping proposals isolated from the dataset. Verified with `npm run build`; build passed with expected large chunk warnings. Ran Node sanity checks confirming the stable importer leaves `Exp10.calculation` and `Exp29.sweep` null while still attaching file refs, the scanner returns workbook metadata and detected experiment labels, and the AI boundary returns a typed stub response with zero proposals.
- Fixed manuscript text changes not participating correctly in Undo/Redo by adding a pending rich-text history batch: normal typing now batches into a single undo step after an 850ms pause, while paste, Enter, Backspace/Delete, toolbar formatting, alignment, save, selection changes, and non-text canvas edits flush/commit the pending text snapshot. Added a history render tick so toolbar Undo/Redo enabled states update after text batches commit. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Extended the Excel-folder importer to parse the new `extra data` workbook set: `MasterTable_updated.xlsx` remains the registry source, and calculation, sweep, reaction-rate, and Parr workbooks now populate `calculation`, `sweep`, `rate_sources`, `parr_data`, `files`, and provenance `sources` for matching `Exp1`-`Exp60` records while excluding `Exp61` because it has no master-table row. Added `scripts/generateLabratData.mjs` to regenerate the bundled dataset from the same parser and write both `src/data/labratData.json` and `public/labratData.json`.
- Moved embedded dataset loading out of the main JS bundle: the app now fetches `public/labratData.json` at startup instead of statically importing the large dataset, keeping the main app chunk manageable after adding parsed Parr traces. Verified with `npm run build`; build passed with expected large chunk warnings, emitted `dist/labratData.json`, and reduced the main app chunk from the 100+ MB inline-data regression back to about 700 kB. Also sanity-checked the regenerated data: 60 experiments present, `Exp61` excluded, `Exp10` has parsed calculation/rate/Parr data, and `Exp29` has parsed calculation/sweep/rate/Parr data with provenance kinds attached.
- Adjusted the Manuscript toolbar text color control to match the requested Google Docs-style icon: a compact square control with a centered `A` and thick color swatch bar underneath, while preserving the existing color picker behavior. Verified with `npm run build`; build passed with large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Split underline and text color into separate Manuscript toolbar controls, added underline as a first-class `textRuns` run style, rendered underline in canvas text boxes, exported underline to editable PowerPoint text runs, and replaced placeholder alignment letters/symbols with cleaner Google Docs-like stacked-line alignment icons. Verified with `npm run build`; build passed with expected large chunk warnings. Generated an underline PPTX fixture, confirmed XML parsed with underline present, confirmed Microsoft PowerPoint COM opened the file with one slide, and confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Restyled the Manuscript rich-text toolbar into a more Google Docs-like compact grouped layout: Save/Undo/Redo, font/size, text style, and paragraph alignment are now visually separated, icon-forward, lighter, sticky, and responsive without changing `textRuns`, undo/redo behavior, or PPTX export. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Implemented the Plan 4 structured rich-text toolbar for manuscript text boxes: added canonical `textRuns` paragraph/run normalization with legacy plain-text migration, a sticky Manuscript toolbar with Save/Undo/Redo/font/size/bold/italic/color/alignment controls, a structured rich-text editor surface for text boxes, manuscript-scoped undo/redo snapshots, overview plain-text extraction from `textRuns`, and PPTX export of editable rich text runs. Passed `npm run build` with expected large chunk warnings. Generated a rich-text PPTX fixture from structured runs, validated XML and styled run markers (`runs=4`, bold/italic present, 28pt present), and confirmed Microsoft PowerPoint COM opened the file with one slide. Manual in-app browser testing was blocked by the recurring Windows sandbox startup failure.
- Removed the remaining manuscript auto-grow behavior from block insertion: once fixed pages exist, manuscript canvas height now comes only from page records instead of chart/text/image block bottoms, and new chart/text/image insertions clamp inside the target page so right-clicking near a page bottom does not create extra pageless space. Updated project persistence normalization so saved page-based manuscripts collapse canvas height back to the last page bottom rather than preserving block-driven bottom padding. Verified with `npm run build`; build passed with expected large chunk warnings. Ran a Node project-normalization sanity check confirming a block below a 900px page no longer expands saved `canvasHeight`, and confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Fixed PowerPoint manuscript export after PowerPoint rejected generated decks: replaced the fragile hand-written OpenXML package writer with `pptxgenjs`, corrected the coordinate pipeline so manuscript pixels convert once into slide inches/EMU, kept text/images as native PowerPoint objects, and kept chart plots as PNGs with editable surrounding labels/legend. Added explicit chart helper imports needed by the export path and installed the `pptxgenjs` dependency. Verified with `npm run build`; build passed with expected large chunk warnings. Generated a three-slide fixture through the patched exporter, validated all slide XML/rels parsed with sane landscape slide geometry, and confirmed Microsoft PowerPoint COM opened the file with three slides. `npm install` reported one high-severity audit issue that was not addressed in this task.
- Removed user-creatable pageless manuscript space and replaced the footer control with a plain `+` page button. Added a one-time manuscript orientation chooser for the first page, persisted `pageOrientationPreference`, inferred orientation from existing first pages, and made future footer/overview page insertions use the locked landscape or portrait size. Updated README persistence notes. Verified with `npm run build`; build passed with expected large chunk warnings. Ran a Node project-normalization sanity check for empty pageless cleanup and orientation persistence. Confirmed `http://127.0.0.1:5173` responds with HTTP 200.
- Added hybrid manuscript page-range export to PowerPoint: the Manuscript sidebar now opens an export modal for page X through page Y, generating one `.pptx` slide per selected page with text boxes and uploaded images as native slide objects and chart plot areas rendered as PNGs with editable surrounding chart labels/legend. Implemented the browser-side PPTX package writer in `src/export/pptxExport.js` without adding dependencies. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200. Manual in-app browser export verification remains blocked by the recurring Windows sandbox startup failure.
- Changed chart insertion defaults so a new chart starts with no selected experiments unless experiments are already staged; removed the old implicit `Exp1`-`Exp4` fallback from the insert-chart draft. Verified with `npm run build`; build passed with expected large chunk warnings.

## 2026-06-06

- Restored direct double-click x-axis label editing on selected chart plot areas: when the inner plot area is selected, categorical x-axis labels show an inline overlay editor that writes through the existing `seriesLabels` inspector-backed rename state. Verified with `npm run build`; build passed with expected large chunk warnings.
- Restored text-box style editing with normalized saved fields for fill color/no-fill, border color, border thickness, and no-border; text boxes now render those styles live on the canvas and expose a Text Box Style inspector section while preserving text editing and selection-frame move/resize behavior. Updated `SelectionFrame` to allow styled outer frames. Verified with `npm run build`; build passed with expected large chunk warnings.
- Expanded the Manuscript workspace layout: the right inspector column is only reserved while a block is selected, and the Workspace Overview sidebar can be hidden/restored with compact controls so the canvas can occupy the full area under the top nav when no side panels are open. Verified with `npm run build`; build passed with expected large chunk warnings.
- Simplified Manuscript page flow so empty manuscripts normalize to no pages/no canvas height, legacy blank auto-pages are removed, the bottom control is now an Add more space menu with pageless space (+800px) and separate landscape page (1600x900) actions, and page geometry now stores y/width/height/orientation while preserving legacy non-empty layouts. Updated README persistence notes. Verified with `npm run build`; build passed with expected large chunk warnings. Ran a Node normalization sanity check for empty, legacy blank, pageless, and non-empty block states. Started Vite and confirmed `http://127.0.0.1:5173` responds with HTTP 200; manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Changed the manuscript footer `+` control to append one blank fixed page immediately instead of opening an add-page menu; overview right-click page controls remain available for before/after/delete actions. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added explicit fixed manuscript pages with project persistence/migration, stacked page rendering, page labels in the workspace and overview, a bottom `+` add-page menu, and overview right-click actions for add-before/add-after/delete. Page deletion now confirms removal of contained blocks, shifts later blocks up, and blocks deletion when a block crosses the target page boundary. Verified with `npm run build`; build passed with expected large chunk warnings. Started Vite and confirmed `http://127.0.0.1:5173` responds with HTTP 200; manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Hardened Manuscript tab rendering against malformed persisted blocks by normalizing block shapes before canvas/overview rendering and only showing the Inspector when the selected block still exists. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://127.0.0.1:5173` responds with HTTP 200. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Limited the topbar Save button to the Manuscript tab while leaving project save behavior and dirty tracking unchanged. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed `http://localhost:5173` responds with HTTP 200.
- Removed the direct in-canvas x-axis label overlay that was blocking Plotly x-axis labels underneath; x-axis renaming now remains through the Inspector and still flows through `seriesLabels` into chart rendering. Verified with `npm run build`; build passed with expected large chunk warnings. Started Vite and confirmed `http://localhost:5173` responds with HTTP 200; manual/browser automation verification was blocked by the recurring Windows sandbox startup failure.

## 2026-06-04

- Updated chart-type colors so selectivity uses screenshot-style pale yellow/pink/blue, conversion uses red pastel, and carbon balance uses green/red pass-fail bars plus a green dashed 95% threshold line while leaving rate and C-number distribution colors unchanged. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed the dev server responds with HTTP 200; manual/browser automation verification was blocked by the recurring Windows sandbox startup failure.
- Split chart label editing into X-axis labels and Legend labels, added direct canvas editing for legend names and category x-axis labels, and kept label overrides in chart opts without changing experiment data. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed the dev server responds with HTTP 200; manual/browser automation verification was blocked by the recurring Windows sandbox startup failure.
- Centralized chart colors into a pastel palette by updating selectivity/fallback constants and replacing conversion/carbon-balance hard-coded trace colors with palette constants. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed the dev server responds with HTTP 200; manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Restored the colorful purple-blue gradient treatment on the `Ask Lab Rat` topbar button while preserving the unified topbar control height and alignment. Verified with `npm run build`; build passed with expected large chunk warnings.
- Moved the experiment-count badge to the left of `Load Excel folder` and aligned the topbar badge/buttons to the same 34px control height and baseline. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added editable chart series labels through the chart inspector, storing optional legend-name overrides in chart opts and applying them to selectivity, rate, distribution, conversion, and carbon-balance traces. Verified with `npm run build`; build passed with expected large chunk warnings.
- Restored the LabRat brand word as the allowed bold exception while explicitly setting left-sidebar filter and manuscript-sidebar headings to normal weight. Verified with `npm run build`; build passed with expected large chunk warnings.
- Removed visible bold UI text by normalizing heavy CSS font weights and replacing visible JSX bold tags with neutral spans; unified topbar action buttons to the same quiet outlined style while keeping the Ask Lab Rat icon and save dirty dot. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed the dev server responds with HTTP 200; manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.

## 2026-06-03

- Added a reusable pastel chart palette, applied pastel open-circle markers to reaction-rate scatter traces, and recolored C-number distribution grouped bars without changing chart type. Verified with `npm run build`; build passed with expected large chunk warnings.
- Normalized selectivity chart traces to a 100% S/L/G basis while preserving raw table/detail values, clamping invalid or negative selectivity components to zero for charting. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed Exp57 and Exp54 chart-normalized S/L/G sums are 100%.
- Added a leading middle-dot separator before the "Your AI Research Assistant" topbar subtitle. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added a close button to the selected-chart assist card with ephemeral per-chart dismissal and event isolation so closing does not trigger analysis. Verified with `npm run build`; build passed with expected large chunk warnings. Confirmed the dev server responds with HTTP 200; manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Changed Ask Lab Rat responses to stream from Anthropic into a live assistant bubble, with chart text insertion enabled only after streaming completes and chat history persistence stripped of transient streaming flags. Verified with `npm run build`; build passed with expected large chunk warnings.
- Cleaned up chart inspector controls: removed redundant legend/Y-title placement buttons, renamed axis-title panels and advanced headings, renamed plot auto-fit, and fixed visible text alignment for chart text layers. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Added durable project persistence with IndexedDB, localStorage migration, topbar Export/Import `.labrat.json` backups, and docs for project storage. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Redesigned the selected-chart Lab Rat prompt as a compact chat-starter card with rat avatar, chart title detail, and Analysis/Trend/Caption chips while preserving existing prompt behavior. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Added browser-style search and combinable quality/data filters to the chart experiment picker, including post-rxn GC, sweep, rate, and carbon-balance filters with a Clear filters action that preserves selection. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added a selected-chart Lab Rat assist bubble beside manuscript charts; clicking it opens Ask Lab Rat and automatically sends the selected-chart analysis prompt. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Wired the Lab Rat expand header button to toggle a full-screen chat panel and reset to drawer mode on close; expanded mode centers readable chat content and keeps the composer at the bottom. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Added Experiment Browser filters for "Has post-rxn GC data" and "Has sweep data" using existing calculation/sweep provenance fields. Verified with `npm run build`; build passed with expected large chunk warnings.
- Aligned Lab Rat chat bubbles so user and assistant message boxes share the same left/right boundaries while avatars remain on opposite sides. Verified with `npm run build`; build passed with expected large chunk warnings.
- Polished the Ask Lab Rat drawer spacing and wrapping: slimmer header/actions, welcome prompts, chart actions, and bottom composer, plus safe wrapping for long user messages inside blue bubbles. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Added a sortable `Sel S/L/G` column to the Experiment Browser using the existing solid/liquid/gas selectivity fields. Verified with `npm run build`; build passed with expected large chunk warnings.
- Moved the top-left "Your AI Research Assistant" subtitle slightly higher for better visual alignment with the LabRat brand. Verified with `npm run build`; build passed with expected large chunk warnings.
- Replaced the Lab Rat assistant inline gear settings with a centered settings modal, added saved writing examples/project background/house rules fields, and included those fields in assistant context. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Changed the Experiment Browser C-bal % and Files table cells to use normal text weight instead of bold badge text. Verified with `npm run build`; build passed with expected large chunk warnings.
- Lowered the top-left rat logo slightly so its visible bottom edge aligns better with the adjacent brand text. Verified with `npm run build`; build passed with expected large chunk warnings.
- Adjusted the top-left brand styling so "Your AI Research Assistant" bottom-aligns with "LabRat" and has a little more spacing from the brand word. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Fixed text-box edit mode so Backspace/Delete edits characters instead of deleting the selected canvas block, and added right-click delete actions for charts, text boxes, and graph/image blocks. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Fixed squished Workspace Overview chart plots by giving the mini Plotly render compact overview-specific margins and tick fonts while preserving the chart layer positions. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Changed Workspace Overview chart blocks from text summaries to read-only miniature chart previews using the same chart layout and Plotly data as the canvas chart. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Restored the Ask Lab Rat assistant UI: the topbar button now shows the rat logo plus "Ask Lab Rat"; the drawer uses the older rat-avatar header, welcome prompts, avatar chat messages, settings-toggle API/model controls, and a selected-chart analysis callout. Repaired several broken mojibake display strings in `src/main.jsx` that became syntax-breaking during this pass. Verified with `npm run build`; build passed with expected large chunk warnings. Started Vite and confirmed `http://localhost:5173` returns HTTP 200. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Fixed malformed mojibake string literals in `src/main.jsx` that caused Vite's "Unterminated string constant" React Babel error; fallback labels now use ASCII text. Verified with `npm run build`; build passed with expected large chunk warnings.
- Renamed the manuscript left sidebar to "Workspace Overview" and replaced single-letter overview marks with compact chart, text, and image previews. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Updated the top-left brand subtitle to "Your AI Research Assistant" and tightened logo/text spacing. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.

- Reworked the manuscript chart inspector/template workflow: chart experiments are now changed through the experiment picker instead of manual Exp# entry, chart template save/apply actions moved to the chart right-click menu, and empty-canvas right-click remains insertion-only. Updated `SelectionFrame` to forward chart context-menu events and added small menu/inspector styles. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.

## 2026-06-02

- Increased the Insert chart modal width to give the chart setup and preview more room. Verified with `npm run build`; build passed with expected large chunk warnings.
- Simplified the "Select experiments for chart" modal into a cleaner wide layout with sort and variable filter controls, a full-width filter dropdown, right-aligned visible/selected counts, a larger scrollable table, and persistent footer actions for Select all visible, Clear selection, and Done. Verified with `npm run build`; build passed with expected large chunk warnings.
- Implemented the manuscript canvas right-click insertion workflow: the canvas context menu now has exactly Insert chart, Insert text box, and Insert image...; Insert chart opens a draft modal with chart type, selected experiment count, preview, and a detailed experiment picker table with search, filters, newest/oldest sort, counts, and checkboxes. Verified with `npm run build`; build passed with expected large chunk warnings. Manual in-app browser verification was blocked by the recurring Windows sandbox startup failure.
- Fixed the top-left logo path for GitHub Pages by using Vite `import.meta.env.BASE_URL`; verified with `npm run build`.
- Configured static GitHub Pages deployment for Vite with `base: "/LabRat/"`, `gh-pages`, deploy scripts, and homepage metadata. Verified `npm run build` outputs `dist/` with `/LabRat/` asset paths and `npm run deploy` published to the `gh-pages` branch.
- Simplified the chart inspector with collapsible sections for chart setup, actions, title, legend, axis titles, and advanced layout controls. Verified with `npm run build`; build passed with expected large chunk warnings.
- Improved selected-chart assistant prompts to request plain manuscript prose and added Markdown cleanup before inserting assistant replies as canvas text boxes. Verified with `npm run build`; build passed with expected large chunk warnings.
- Replaced the Apache 2.0 license with the PolyForm Noncommercial License 1.0.0 and updated `README.md`.
- Added Apache License 2.0 in `LICENSE` and documented the license in `README.md`.
- Added selected-chart assistant context: selecting a manuscript chart exposes chart-aware LabRat quick actions for description, key trend, and figure caption, and chart-sourced assistant replies can be inserted as manuscript text boxes. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added persisted manuscript canvas height under `labrat_canvas_height`; the canvas now auto-grows when blocks extend beyond the current page and does not auto-shrink. The overview minimap scales to the saved canvas height. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added chart templates for the manuscript canvas: selected charts can be saved as visual templates from the inspector, persisted under `labrat_chart_templates`, and inserted from the canvas right-click menu using current staged experiments. Verified with `npm run build`; build passed with expected large chunk warnings.
- Added preset font-family controls for manuscript text boxes and chart text layers including title, legend, X-axis title, and Y-axis title. Verified with `npm run build`; build passed with expected large chunk warnings.
- Improved the manuscript canvas overview so chart, text, and image blocks have distinct colors, compact type marks, hover labels, and a legend. Verified with `npm run build`; build passed with expected large chunk warnings.
- Changed the manuscript page so the left sidebar shows a clickable whole-canvas overview, and moved chart experiment staging into the canvas right-click window alongside insert actions.
- Verified with `npm run build`; build passed with expected large chunk warnings for Plotly/XLSX.
- Started Vite at `http://localhost:5173` and confirmed the page responds with HTTP 200. In-app browser verification was blocked by a repeated node/browser sandbox startup failure.
- Initialized git for the workspace, ignored generated folders and local archived source material, and prepared the app/docs for an initial commit.
- Added this progress log and updated `AGENTS.md` so future agents record requests and changes in `PROGRESS.md`.
- Added `README.md` and `AGENTS.md` to establish human and agent working context for the LabRat React app.
- Refreshed dependencies with `npm install` because the existing Windows install was missing Rollup optional packages.
- Verified the app with `npm run build`; build passed with expected large chunk warnings for Plotly/XLSX.
- Noted existing issues: UI mojibake, one npm high-severity audit finding, no formal test suite, and browser-direct Anthropic API usage.

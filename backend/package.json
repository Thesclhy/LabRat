{
  "name": "labrat-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend scan-only import pipeline for LabRat Phase 1",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}

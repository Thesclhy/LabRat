# LabRat Backend

This folder is reserved for the Phase 1 backend scan-only pipeline.

Current checkpoint:

- isolated backend folder created

Next checkpoints:

- add backend package setup
- add backend dev/start/test scripts
- add minimal backend server
- add `POST /api/import/scan`

Do not implement Phase 2 normalization or Phase 3 AI/chart proposal work here until Phase 1 is complete.
